# 整理計画

## 背景・方針

Qiitaに書き溜めた記事をAPIで取得すると、投稿順のフラットな一覧になり、後から特定の記事を探したり、関連記事をまとめて読んだりしにくい。記事を「カテゴリ」と「シリーズ」の2軸で整理し、見通しを良くする。

- カテゴリ分類: 記事1件ごとに `articles/{category}/{slug}.md` へ配置する。slugはタイトル・本文から意訳した短いkebab-case。
- インデックスファイル（`articles.tsv`）は手で保守せず、`articles/` を実際に走査して機械生成する。`articles/` を正とし、インデックスはそこから再生成できる使い捨てのビューとして扱う。依存関係チェックなしで再生成できるMakeターゲットにしておくと、手動修正後の再生成が楽になる。
- シリーズ化: 連載記事は `series/{slug}/` にまとめ、`articles/`（および`articles.tsv`）の対象外とする。各ディレクトリの `README.md` を正データとする。
- 分類・抽出に使った中間ファイル（分類結果、シリーズ抽出結果など）は、記事側への反映が完了すれば削除してよい。LLM呼び出しの参照コードとしてスクリプト自体は残しておく。

## 作業フロー

1. 記事データを取得し、フロントマター付きMarkdownとして1記事1ファイルに展開する。
2. 本文・タイトルからカテゴリを判定し、`articles/{category}/{slug}.md` の形で配置する。
3. `articles/` を走査してインデックス（`articles.tsv`）を生成するスクリプトを用意する。
4. 一部の記事は本文冒頭に関連記事へのリンク一覧（「〜一覧」「〜まとめ」）がありシリーズ関係が明示されている。これをローカルLLMで抽出し、機械可読な形式（jsonl等）に保存した後、人間可読な形式（md）に整形して手動確認する。
5. 確認・確定したシリーズから順に `series/{slug}/` へ移行する。
   1. `articles/{category}/` から `series/{slug}/` へ記事を `git mv`。連載順が確定している本編は `01-slug.md`, `02-slug.md`, ... と連番を付与し、解答記事は `01a-slug.md` のように同じ番号にサフィックス `a` を付ける。応用編・番外編（連載順に含まれない記事）は連番なしで同じディレクトリに置く。
   2. `series/{slug}/README.md` に目録を作成する（仕様は下記）。
   3. 最上位の `series/README.md` にリンクを追記する。
   4. 記事が複数シリーズにまたがる場合、主たる帰属先ではない側は移動せず参照パスだけ更新する。主たる帰属先が定まらない/両シリーズにとって本質的なメンバーの場合は個別に判断する。
   5. 移動によって他の記事が参照しているパスが変わることがあるため、旧パスで全体をgrepして参照漏れがないか確認する。
   6. インデックス（`articles.tsv`）を再生成する。
6. 分類・抽出作業に使った中間ファイルを削除する。

## series/{slug}/README.md の仕様

```markdown
# {シリーズタイトル}

1. [{記事タイトル}](01-{slug}.md) `{20桁ID}`
   - [【解答例】{記事タイトル}](01a-{slug}.md) `{20桁ID}`
2. ...

## 応用編

- [{記事タイトル}]({slug}.md) `{20桁ID}`

## 番外編

- [{記事タイトル}]({slug}.md) `{20桁ID}`
```
