# 整理計画

## 背景・目的

`items/` に281件のQiita記事が `{id}.md`（Qiita記事ID）というフラットなファイル名で並んでいる状態から、ディレクトリによるカテゴリ・シリーズ分類および適切なファイル名への変更を行い、記事を整理・検索しやすくする。

分類作業（シリーズ構成抽出、カテゴリ判定、スラッグ決定など）はローカルLLMを活用して自動化・補助する。

## 現状

- 記事はHaskell/F#などの言語入門・数学（四元数・八元数）・音声合成・生成AI活用・レトロ計算機の機械語調査・Web技術など多岐にわたる。
- 各記事ファイルのフロントマターには `created_at`（作成日時）、`title`、`tags` 等が付与されている（`extract.py` がQiita APIのJSONから生成）。
- フロントマターの内容（`id`・`tags`等）はQiita側のデータそのものなので書き換えない。ファイル名およびディレクトリ配置を変更する。
- 一部の記事は本文冒頭が関連記事へのリンク一覧（「〜一覧」「〜まとめ」）になっており、シリーズ関係が明示されている。

## ファイル命名規約

ディレクトリによる構造化を基本とする。

1. **シリーズ記事**: `{SERIES}/{NUM}-{slug}.md`
   `SERIES` はシリーズを表すディレクトリ名（例: `haskell`, `wiktionary`, `sapi`）。`NUM` は昇順に振る2桁連番（`01`, `02`, ...）。日付は入れない。
2. **単発記事**: `{カテゴリ}/{YYYYMMDD}-{slug}.md`
   シリーズを構成しない単発記事は、テーマ別のカテゴリディレクトリ（例: `math`, `ai`, `web`, `misc` 等）にまとめる。`YYYYMMDD` はフロントマターの `created_at` の日付部分。

## カテゴリ・シリーズ候補

タグ集計（言語名、数学系、音声合成系、生成AI系タグの出現頻度等）をもとにした候補。LLMによる分類作業の中で見直す。

- **Haskell系** — シリーズ・カテゴリ候補
- **Wiktionary系・音声合成系（SAPI/SSML）** — 同一テーマが連続するシリーズ・カテゴリ候補
- **数学** — 四元数・八元数・クリフォード代数・線形代数（カテゴリ候補）
- **生成AI** — Gemini/Ollama/MCP/RAG/BitNet/Claude等（カテゴリ候補）
- **レトロ計算機** — VAX/PDP-11/8086/S390などの機械語調査（カテゴリ候補）
- **Web/フロントエンド** — React/WebAssembly/Promise/HTML（カテゴリ候補）
- **環境・ツール** — WSL/uv/msys2/Git等の環境構築系（カテゴリ候補）
- **misc** — 上記に収まらない単発記事

## スラッグ命名方針

ファイル名の内容部分は英字の短いkebab-caseとする（日本語タイトルからの意訳でよく、既存の英語タグ・技術用語（`Haskell`, `SSML`等）はそのまま使用する）。

## 作業フロー

LLMを用いた解析・分類処理は互いに独立しており順不同で実行できる。

1. **本文からのシリーズ構成抽出 (`scripts/extract_series.py`)**
   - 本文冒頭の関連記事リンク群をローカルLLMで解析し、同一シリーズに属する記事IDグループを抽出して `series.jsonl` に出力する。
   - **`series.jsonl` パース時の注意点**: 先に処理した記事 A が単独判定（`member_ids: []`）された後、後から処理した記事 B が A とのグループ判定（`member_ids: [...]`）を出力・追記するケースがある。そのため、単独判定の行は無視し、順番にグループ判定された記事にフラグを付けていき、全行読み終えた時点でフラグが付いていない記事を単独記事と判定するアルゴリズムとする。
2. **全記事の分類・SLUG生成 (`scripts/classify.py`)**
   - 全記事のタイトル・タグ・本文冒頭50行をローカルLLMに渡し、カテゴリ名 (`category`) とスラッグ (`slug`) を判定して `classified.tsv` に出力する。
3. **レビューと適用**
   - 抽出・分類結果（`series.jsonl`, `classified.tsv`）をレビュー・必要に応じて手動調整し、それらに基づいてディレクトリ作成およびファイル移動（リネーム）を行う。

## 今後の課題

- 分類結果（`classified.tsv` / `series.jsonl`）の確認と必要に応じた手動修正
- ディレクトリ作成・ファイル移動スクリプトの作成と実行
- 各ディレクトリへの README.md（記事索引）の作成
