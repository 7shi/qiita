# 整理計画

## 背景・目的

`items/` に281件のQiita記事が`{id}.md`（Qiita記事ID）というフラットな名前で並んでいるだけで、内容による構造化ができていない。hypercomplexで採用している「ディレクトリ＝シリーズ」の運用をこちらにも導入し、記事を探しやすくする。

分類作業（シリーズ判定・カテゴリ判定・スラッグ決定）はローカルLLMに行わせる想定。本ファイルはその前提となる分類方針とファイル命名規約を定める。

## 現状

- 記事はHaskell/F#などの言語入門・数学（四元数・八元数）・音声合成・生成AI活用・レトロ計算機の機械語調査・Web技術など多岐にわたる。
- フロントマターに`created_at`（作成日時）、`title`、`tags`が付与されている（`extract.py`がQiita APIのJSONから生成）。
- フロントマターの内容（`id`・`tags`等）はQiita側のデータそのものなので書き換えない。ファイル名とディレクトリ配置のみを変更する。
- 一部の記事は本文冒頭が関連記事へのリンク一覧（「〜一覧」「〜まとめ」）になっており、シリーズ関係が明示されている（例: `b6cbb7df2dd969c84f49.md`「Haskellの実験メモ一覧」）。

## ファイル命名規約

hypercomplexと同様、ディレクトリ＝シリーズを基本とする。

1. **シリーズ記事**: `{SERIES}/{NUM}-{slug}.md`
   `SERIES`はシリーズを表すディレクトリ名（例: `haskell`, `wiktionary`, `sapi`）。`NUM`は`created_at`の昇順に振る2桁連番（`01`, `02`, ...）。日付は入れない。
2. **単発記事**: `{カテゴリ}/{YYYYMMDD}-{slug}.md`
   シリーズを構成しない記事は、テーマ別のカテゴリディレクトリ（hypercomplexの`misc/`に相当、複数に分割してもよい）にまとめる。`YYYYMMDD`はフロントマターの`created_at`の日付部分。

## カテゴリ・シリーズ候補

タグ集計（Haskell/F#/Python等の言語名、数学系、音声合成系、生成AI系タグの出現頻度）をもとにした初期案。ローカルLLMによる分類作業の中で過不足が見つかれば見直す。

- **Haskell系** — 「〜超入門」「【解答例】〜超入門」のペアを含む一群。シリーズディレクトリになる見込み
- **Wiktionary系・音声合成系（SAPI/SSML）** など、同一テーマが連続する一群もシリーズディレクトリの候補
- **数学** — 四元数・八元数・クリフォード代数・線形代数（単発記事のカテゴリ）
- **生成AI** — Gemini/Ollama/MCP/RAG/BitNet/Claude等（単発記事のカテゴリ）
- **レトロ計算機** — VAX/PDP-11/8086/S390などの機械語調査（単発記事のカテゴリ）
- **Web/フロントエンド** — React/WebAssembly/Promise/HTML（単発記事のカテゴリ）
- **環境・ツール** — WSL/uv/msys2/Git等の環境構築系（単発記事のカテゴリ）
- **misc** — 上記に収まらない単発記事

シリーズと単発記事のカテゴリは、どちらも同じ階層のディレクトリとして並置する（hypercomplexで`lie/`や`oct/`と`misc/`が並ぶのと同じ構造）。

## シリーズ判定基準

タイトルだけでは判断できない記事があるため、ローカルLLMは本文を読んで判定する。

- タイトル末尾に`(2)`のような回数表記があるもの
- 「〜超入門」と「【解答例】〜超入門」のような明確な対構成
- 同一テーマ（Wiktionary調査、GIF解析、パーサコンビネータ実装など）が時系列で連続する一群
- **本文冒頭が関連記事へのリンク一覧になっている「一覧」「まとめ」系の記事**（例:「Haskellの実験メモ一覧」）は、そこに列挙された記事群をシリーズとみなす最有力の手がかりとして優先的に参照する

単発かシリーズかの最終判断、およびシリーズディレクトリ名の決定はローカルLLMに委ねる。

## スラッグ命名方針

hypercomplex同様、ファイル名の内容部分は英字の短いkebab-caseとする（新規slugはファイル名の内容部分を略したものを基本とする）。日本語タイトルからの意訳でよく、既存の英語タグ・技術用語（`Haskell`, `SSML`等）はそのまま使う。

これはMathlogの参考文献slug（`[[slug]]`）とは別物で、qiita記事には引用ラベルの概念がないため、あくまでファイル名の一部として運用する。

## 数学カテゴリとhypercomplexとの関係

`数学`カテゴリの記事（四元数・八元数・クリフォード代数等）はhypercomplexとテーマが重なるが、両リポジトリを直接参照させる連携は行わない。

- qiita側は誤りの修正など最低限のメンテナンスに留め、独立したアーカイブとして扱う。
- 内容を発展させたい場合は、qiita記事をそのまま参照するのではなく、必要に応じて書き直した上でhypercomplex側に個別の記事として取り込む。

## 作業フロー

1. ローカルLLMが全記事のタイトル・タグ・本文（「一覧」系記事があればその関連記事リンクも含む）を読み、`シリーズ有無/ディレクトリ名/NUM`または`カテゴリ/slug`を判定し、分類結果を一覧表（TSV等、`id`をキーとする）として出力する。
2. 分類結果をレビューし、必要に応じて手で修正する。
3. 分類結果に基づいて実際のディレクトリ作成・ファイル移動（リネーム）を行うスクリプトを別途用意し、適用する。

各ディレクトリへのREADME.md（記事索引）整備は、リネーム後の課題として別途行う。

## 今後の課題

- カテゴリ・シリーズ候補の見直し（分類作業中に判明した過不足の反映）
- 各ディレクトリのREADME.md作成
- ルートのREADME.mdを新しいディレクトリ構成に合わせて更新
