# Haskell Freeモナド 超入門 プラン

記事番号は 16、ファイル名は `16-free-monad.md`（予定）。

**状態: 構成の方針は決着（2026-08-07）。本文未着手。検証コード未作成。**

以下は執筆前の構成案。13〜15 回と同じく、決着した判断は「決定事項」に置く。
**執筆・推敲時の判断で覆した点は「執筆時の変更点」に追記していく。**

## 公開方針

[README.md](README.md) の「公開方針（13回以降）」に従う。14・15 回と同じ扱い。

- **執筆はこの Qiita 側リポジトリで行い、公開は Zenn。** Qiita には投稿しないので
  フロントマターの `id`・`url` は空のままにする。
- 補足・折りたたみは最初から **Zenn 記法**（`:::message`・`:::details`）を使う。
- 完成後、Zenn リポジトリ（`~/repos/zenn`）へ複製し、[ZENN.tsv](../../ZENN.tsv) に追記する。
- 練習問題は 13〜15 回に倣い、**解答を別記事に分けず `:::details` で本文に統合**し、
  関連する節の末尾に分散させる。
- 検証コードは `check/16-*/` に置き、各ディレクトリに `README.md` を入れる。

## 記事の狙い

**ゴールは「組み立てと解釈の分離」**（決定事項 2）。

Free モナドの核心は [README.md](README.md) の「構想」に既に書いてある。

> **組み立て（Freeモナド側）と解釈（インタプリタ側）を分離するのが核心**。`>>=` は命令を
> つなげるだけで実行しない。できあがった手順書を辿って実際の処理に変換するインタプリタは
> 別関数として用意し、同じ手順書を本番実行用・テスト用モック・ログ収集用など複数の
> インタプリタで使い回せる。

15 回までのモナドは、`>>=` でつないだ時点で**意味が決まっていた**。
`IO` なら実行され、`Maybe` なら失敗が伝播し、`State` なら状態が流れる。
Free モナドは `>>=` に**何の意味も与えない**。ただ命令をデータとしてつなぐだけで、
意味づけはインタプリタに委ねる。ここが今までのモナドとの決定的な違いになる。

### この回の一行の主張

15 回の最後に自作した `Tree` が、そのまま Free モナドだった。

| 15 回の題材 | Free での表し方 |
|---|---|
| `data Tree a = Leaf a \| Node (Tree a) (Tree a)`（本文 `## Tree`）| `Free Two`（`data Two x = Two x x`）|
| `data Rose a = Leaf a \| Node [Rose a]`（練習【問4】）| `Free []` |
| `data Pair a = Pair a a`（練習【問1】【問2】）| 上の `Two` と同じ形 |

15 回はこれを**意図的に伏せてある**（15 回決定事項 9。先に言うと `Tree` が踏み台に見え、
自分で `>>=` を書く達成感が薄れるため）。**16 回はその回収から入る。**

> **枝の形が違うだけで `>>=` の中身は同じだった。その「枝の形」を型引数 `f` に
> くくり出したものが Free モナドである。**

`Tree` と `Rose` の `>>=` を並べると、違うのは枝を辿る部分（`Node l r` の左右を
再帰するか、`map` でリストを回すか）だけで、そこはちょうど `fmap` の仕事である。

```hs
Node l r >>= f = Node (l >>= f) (r >>= f)     -- Tree
Node ts  >>= f = Node (map (>>= f) ts)        -- Rose
Free g   >>= f = Free (fmap (>>= f) g)        -- Free
```

**Free モナドが `Functor` を要求する理由がここでそのまま出る。**
15 回の階層（`Functor` → `Applicative` → `Monad`）を経た直後の題材として噛み合う。

### 記事の導入文（案・PREFACES.md 用）

> Haskell ではモナドと呼ばれる部品を組み合わせてプログラムを作ります。**Freeモナド**は
> `>>=` に意味を与えず、命令をデータとしてつなぐだけのモナドです。組み立てた手順書は
> 後からインタプリタで解釈します。木構造の一般化として導入し、ジェネレーターを作ります。

⚠ 「圏論には言及しない」方針（README.md「スタイル」）は 16 回でも維持する。
「自由」は自由モノイド（＝リスト）との対比で説明し、
「自由生成（free construction）」という語だけ由来として 1 行出す（決定事項 4）。

## シリーズ内で先送りされた内容（16 回が回収先）

**執筆時は必ず各箇所を開いて、当時の言い回しに繋がる形で書くこと。**

| 箇所 | 先送りされた内容 | 16 回での扱い |
|---|---|---|
| 15 回 `## Tree`・練習【問4】 | `Tree` = `Free Two`、`Rose` = `Free []` を伏せた（15 回決定事項 9）| **回収。この回の入口そのもの** |
| 14 回決定事項 9 | `DeriveFunctor` は「16 回で動機が立ってから導入する」| **回収**（決定事項 6）|
| 13 回 `## ジェネレーター` | `Cont` でジェネレーターを実装した（生産専用版が本線、双方向版は不採用）| **同じものを Free で作り直す。本文の題材**（決定事項 7）|
| 15 回 16 回への引き継ぎ | codensity モナド・Church エンコード（13-PLAN「今回は入れなかった話」の 3）| **名前も出さない。17 回以降へ**（決定事項 10）|
| 15 回 16 回への引き継ぎ | `Traversable`・`Alternative`・`Foldable`・`MonadFail` | **16 回では扱わない**（決定事項 13）|
| `07-list-monad.md:263`・`:392` | `join` は既習（練習【問2】で型から仕様を推定させ、本文でも使用）| `Free` の `>>=` の説明に使える足場 |

## 構成案

### 1. 木を一般化する（15 回の回収）

- **入口は 15 回の `Tree` と `Rose` の `>>=` を並べること。** 上記「一行の主張」の 3 行。
- 枝の形を型引数にくくり出す。

  ```hs
  data Two x = Two x x

  data Free f a = Pure a | Free (f (Free f a))
  ```

  - `Pure` が `Leaf`、`Free` が `Node` に対応する。
  - `Free Two Int` が `Tree Int` と同じ形であることを実際に値を組んで見せる。
  - `Free [] Int` が `Rose Int` に対応する。
- **`f` の種が `* -> *` であること**（14 回の種の回収）。`Free` 自体の種は `(* -> *) -> * -> *`。
  `:k Free` を GHCi で見せると効く。
- `instance` を書く。**15 回の定型（`fmap = liftM` / `(<*>) = ap`）がそのまま使える。**

  ```hs
  instance Functor f => Monad (Free f) where
      Pure a >>= k = k a
      Free g >>= k = Free (fmap (>>= k) g)
  ```

  - **`Functor f =>` という制約が付く理由**をここで説明する。枝を辿るのに `fmap` が要る。
    15 回で `Functor` を階層の一番下に置いた意味が、ここで実用の形で出る。
  - `instance` 側の制約は 14 回（`instance Show a => Show [a]`）で既習。
- ⚠ ここまでは「木の一般化」で閉じていて、まだ DSL の話は出さない。
  **`Tree` が Free だった、という驚きだけで 1 節を持たせる。**
- **末尾に練習【問1】**（`Free Two` で 15 回の `Tree` を組み直す）。
- ⚠ **`f` を差し替えると既知のモナドが出るという一覧表は作らない**（決定事項 11）。
  `Tree`・`Rose` の 2 つで閉じる。

### 2. 枝に意味を持たせる（DSL への転回）

**ここが記事の転回点。** 1 節では `f` を「枝の形」と呼んでいたが、
`f` を**命令の型**と読み替えると、Free モナドは命令の並びを表すデータになる。

- 題材は**ジェネレーター**（決定事項 7）。13 回で `Cont` を使って作ったものを
  Free で作り直す。**生産専用**（`yield` するだけ。13 回の本線と同じ範囲）。

  ```hs
  data GenF o next = Yield o next
  ```

- **`next` が「続き」であること**を強調する。`Yield` は値を 1 つ出した後の続きを持つ。
  → **13 回との接続はここ**（決定事項 14）。13 回は継続を `Cont` で捕まえたが、
  Free では続きがデータの中に直接入っている。**1〜2 段落 + 行末リンク**に留め、
  13 回を読んでいない読者にも分かるよう独立して説明する（README.md「スタイル」）。
- `instance Functor GenF` を手で書く。

  ```hs
  instance Functor (GenF o) where
      fmap f (Yield o next) = Yield o (f next)
  ```

  - **手で書いた後に `DeriveFunctor` を出す**（決定事項 6）。「構造から機械的に決まるので
    コンパイラに書かせられる」という 14 回の `deriving` の議論の続きになる。
    `{-# LANGUAGE #-}` の書き方自体は 06 回 `:217` で説明済みなので繰り返さない。
- **`liftF` で 1 命令を `Free` に持ち上げる。**

  ```hs
  liftF :: Functor f => f a -> Free f a
  liftF c = Free (fmap Pure c)
  ```

- スマートコンストラクタを定義して `do` で手順書が書けるようになる。

  ```hs
  type Gen o = Free (GenF o)

  yield :: o -> Gen o ()
  yield x = liftF (Yield x ())

  count = do
      yield 1
      yield 2
      yield 3
  ```

  - **この `count` はまだ何もしない**という点を強く言う。`do` で書いたのに実行されない。
    値は `Gen Int ()` という**データ**である。
  - 無限の手順書（`mapM_ yield [0 ..]`）も組める。組むだけなら止まらない。

### 3. インタプリタ（**この回の見せ場**）

- **本文は `toList` の 1 本**（決定事項 8）。手順書を辿って `Yield` の値を集める。

  ```hs
  toList :: Gen o a -> [o]
  toList (Pure _)           = []
  toList (Free (Yield o k)) = o : toList k
  ```

  - **13 回の `Cont` 版 `toList` と並べる。** あちらは継続を呼び出して駆動する必要が
    あったが、Free 版はデータを辿るだけの 2 行になる。
  - 無限の手順書に `take 5` が効くこと（遅延評価）。13 回の `nats` と同じ確認。
- **`>>=` が意味を持たないおかげで、意味を後から差し替えられる**ことを述べる。
  同じ `count` を `IO` で走らせるインタプリタも書けることを散文で示し、
  **実装は練習【問2】に回す**（決定事項 8）。
- ⚠ **ここで「これが `IO` と何が違うのか」に答える。**
  `IO` で書いたら実行するしかないが、Free なら中身を検査でき、モックにも差し替えられる。
  03 回・06 回の「アクションは実行される」との対比になる。
- **末尾に練習【問2】**（同じ手順書を走らせる `IO` 版インタプリタ）。

### 4. 「自由」とは何か

- **自由モノイドとの対比で説明する**（README.md「構想」に既述）。
  - リストは「モノイド則だけを満たし、それ以外の性質を持たない」構造。
    `[1] <> [2]` は `[1,2]` になるだけで、足し算にも掛け算にもならない。
    後から「どう畳むか」を `foldr` などで決められる。
  - Free モナドはこれのモナド版。**モナド則だけを満たし、それ以上の意味づけを持たない。**
    意味を後から決められるのは、意味を持っていないから。
- 14 回の `Monoid`・15 回のモナド則（`>=>` で単位元と結合法則に書き直した節）が
  そのまま足場になる。**「単位元と結合法則を持つ構造はモノイド」で締めた 15 回の続き。**
- **「自由生成（free construction）」という語を 1 行だけ出す**（決定事項 4）。
  由来の説明に留め、圏論の語は出さない。

### 5. テレタイプ DSL（練習【問3】【問4】）

**ジェネレーター以外の DSL を読者に作らせる。** 独立した節にするか、
構成案 3 の後に練習として並べるだけにするかは執筆時に判断する。

```hs
data TeletypeF next
    = PutLine String next
    | GetLine (String -> next)
```

- **`GetLine (String -> next)` が関数を持つ**のが `GenF` との違い。
  `fmap f (GetLine k) = GetLine (f . k)` と関数合成になる。
  ジェネレーターでは `next` が素の値だったので、ここで一段深くなる。
- 【問3】命令の型・`Functor`・スマートコンストラクタを書いて手順書を組む。
- 【問4】その手順書の**純粋インタプリタ（テスト用モック）**を書く。
  入力をリストで与え、出力をリストで集める。`IO` を使わずに振る舞いを検査できる。
  → **「テスト用モックが自然に書ける」という Free の実利を読者の手で作らせる。**
- ⚠ Brainfuck DSL 案は**不採用**（決定事項 9）。ループ命令の設計が重く、練習に向かない。

### 6. 性能の注意（`:::message` で数行・決定事項 10）

- `>>=` を左結合で重ねると、そのたびに木を辿り直すので遅くなる。
  リストの `++` を左結合で重ねるのと同じ現象で、**07 回のリストの感覚がそのまま使える。**
- ⚠ **対策（Church エンコード・codensity・`FreeT`）は名前も出さない。**
  13-PLAN「今回は入れなかった話」の 3 で「置くならこの回」とされていたが、
  超入門の射程を超えるので 17 回以降へ送る。
- ⚠ まとめ節に次回予告は書かない（決定事項 12）。

### 7. まとめ

- 構成案 1 の対比表（`Tree` = `Free Two`）に戻る。
- 一行の主張に戻る。「`>>=` が意味を持たないから、意味を後から選べる」。
- ⚠ **次回予告は書かない**（決定事項 12）。

## free パッケージの扱い

**原理は定義を引用しながら説明し、実用ではパッケージを使う**（決定事項 5）。

- 本文では `data Free f a = Pure a | Free (f (Free f a))` と `liftF` の定義を示して
  仕組みを説明する。`instance` も自分で書いて見せる。
- その上で「実用では [free](https://hackage.haskell.org/package/free) パッケージを使う」と
  案内し、対応する API（`Free`・`liftF`・`foldFree`・`iterM` など）を対応表で示す。
- ⚠ **`free` はシステムの GHC には入っていない**（`ghc-pkg list` で確認。
  GHC 9.6.6 / mtl 2.3.1 / transformers 0.6.1.0 のみ）。**stack で導入して検証する**
  （決定事項 5）。cabal は環境に入っていない。
- ✅ **stack の共有ライブラリエラーは修復済み**（2026-08-07）。原因は pacman の部分アップグレードで
  `haskell-unicode-data` など haskell-* パッケージ群（189個）が `stack` パッケージより古いまま
  だったこと。`haskell-*` パッケージのみを対象に `pacman -S` で更新して解消した
  （`-Syu` は稼働中プロセスへの影響を避けるため見送った）。
- ✅ **執筆環境の resolver は `lts-22.28`。** システムの GHC（9.6.6）と一致するため
  `--system-ghc` が使える（`stack setup --system-ghc --resolver lts-22.28` で確認済み）。
- ✅ **記事の掲載コードに `stack script` ヘッダは書かない。実行コマンド側で
  `--resolver`・`--package` を指定する方式に決定**（2026-08-07）。理由は 2 つ。
  - ソースコードが素の Haskell のまま保てる（15 回までの掲載コードと見た目が揃う）。
  - 記事で「stack で動かす場合はこう起動する」という 1 行を示せば済み、
    導入手順の説明と掲載コードを分離できる。

  ```
  stack script --resolver lts-22.28 --package free ファイル名.hs
  ```

  - `stack runghc --package free -- ファイル名.hs` は本環境では `Setup.hs` の
    ビルドに失敗した（`Cabal`/`base` のパッケージDBが `--system-ghc` 併用時に
    壊れる模様）。**`stack script` の CLI 引数指定を使う。**
  - 記事に載せる resolver の値は執筆時点のものを使う（`lts-22.28` は 2026-08-07 時点の確認値。
    実際に記事を書く時点で最新 LTS を確認し直す）。
- ✅ **`free` パッケージの動作確認済み**（`check/16-free/test.hs`）。`Control.Monad.Free` の
  `Free`・`Pure` で `Two`（`Functor` を `deriving`）を使い 15 回の `Tree` 相当の木を組んで
  `toL` で走査し、`stack script --resolver lts-22.28 --package free test.hs` で
  `[1,2,3]` を得た（ソース側にヘッダは無し）。
- ⚠ パッケージ版の `Free` は本文で示す定義と**コンストラクタ名が同じ**（`Pure`・`Free`）
  だが、`Applicative` の実装など細部が異なる。差分は要調査（TODO）。

## 練習（4 問、解答は `:::details` で本文に統合）

13〜15 回に倣い、関連する節の末尾に分散させる。

| # | 位置 | 題材 | 狙い |
|---|---|---|---|
| 問1 | 構成案 1 末尾 | `Free Two` で 15 回の `Tree` と同じ木を組み、`>>=`（接ぎ木）が同じ結果になることを確かめる | 一般化が本当に元の型を含んでいることを手で確認する |
| 問2 | 構成案 3 末尾 | 本文の手順書を走らせる `IO` 版インタプリタを書く | **「手順書は 1 つ、解釈は複数」を読者の手で実演する**（決定事項 8）|
| 問3 | 構成案 5 | テレタイプ DSL の命令の型・`Functor`・スマートコンストラクタ | 関数を持つ命令（`GetLine`）の `fmap` を書く |
| 問4 | 構成案 5 | 問3 の手順書の純粋インタプリタ（テスト用モック）| Free の実利を手で作る。`IO` なしで振る舞いを検査できる |

- 問1 は本文の裏取り、問2 は本文の続き、問3 → 問4 は連続した題材
  （15 回の問1 → 問2 と同じ形）。

## 決定事項

**覆す場合は理由を確認してから。** 1〜14 は 2026-08-07 に決定。

1. **公開は Zenn、執筆は Qiita 側リポジトリ。** 練習の解答は `:::details` で本文に統合。
2. **ゴールは「組み立てと解釈の分離」。** 木の一般化（構成案 1）で閉じる短い記事にはしない。
   構成案 2・3 が本体になり、15 回と同程度の長さになる。
3. **入口は 15 回の `Tree`・`Rose` の回収**（15 回決定事項 9 の裏返し）。
4. **圏論には言及しない**（README.md「スタイル」。13〜15 回と同じ）。
   ただし**「自由生成（free construction）」という語は 1 行出す。**
   自由モノイド（リスト）との対比が説明の本体。
5. **原理は定義を引用しながら説明し、実用では `free` パッケージを使う。**
   検証コードは **stack で `free` を導入して**書く（システムの GHC には未導入。
   cabal は環境に無い）。
6. **`DeriveFunctor` を手書き → `deriving` の順で出す。**
   `{-# LANGUAGE #-}` プラグマ自体は 06 回 `:217`（`UnboxedTuples`・`MagicHash`）で
   説明済み・09 回でも使用済みなので、**プラグマの書き方は説明し直さない。**
   シリーズで初めて「便利のための言語拡張」を出す回になる。
7. **本文の DSL 題材はジェネレーター（生産専用）。** 13 回の `Cont` 版と対比する。
   双方向版（`Yield o (i -> next)`）は本文にも練習にも入れない
   （13 回でも不採用になった範囲）。
8. **本文のインタプリタは `toList` の 1 本のみ。`IO` 版は練習【問2】。**
   「解釈は複数」は散文で述べ、実装は読者に書かせる。
9. **練習のもう 1 本の DSL はテレタイプ。** Brainfuck 案は不採用（ループ命令の設計が重い）。
10. **性能（左結合の遅さ）は `:::message` で数行。**
    codensity・Church エンコードは**名前も出さない。**
11. **`f` を差し替えると既知の型が出る一覧表は作らない。** `Tree`・`Rose` で閉じる。
12. **まとめ節に次回予告を書かない**（14 回推敲時のユーザー指示）。
13. **`Alternative`・`Foldable`・`MonadFail`・`Traversable` は 16 回では扱わない**
    （15 回決定事項 15・引き継ぎ）。主題（組み立てと解釈の分離）から外れる。
    回収先は 17 回以降へ持ち越す。
14. **13 回（継続）との接続は 1〜2 段落 + 行末リンク。**
    `Yield o next` の `next` が「続き」であることの説明に使い、
    13 回を読んでいない読者にも分かるよう独立して説明する。

## 前提知識

**15 回までで揃っている。新規に前提となる材料はない。**

| 材料 | 出典 |
|---|---|
| `data` / `newtype` / レコード構文 / 再帰的なデータ型 | `02-algebraic-data-types.md` |
| `{-# LANGUAGE #-}` の書き方 | `06-io-monad.md:217`（`UnboxedTuples`）・`09-state-monads.md` |
| `join` | `07-list-monad.md:263`・`:392` |
| `class` / `instance` / スーパークラス / 種（`:k`）| `14-type-classes.md` |
| `instance` 側の型クラス制約（`instance Show a => Show [a]`）| 〃 |
| `deriving` が「インスタンス定義の自動生成」であること | 〃 |
| `Functor` / `Applicative` / `Monad` の階層・`fmap`・`pure`・`>>=` | `15-monads-and-friends.md` |
| 定型（`fmap = liftM` / `(<*>) = ap`）| 〃 `## 3 段まとめて書く定型` |
| モナド則（`>=>` による書き直し）・`Monoid` との繋がり | 〃 `## モナド則` |
| 自作モナド `Tree`・`Rose` | 〃 `## Tree`・練習【問4】|
| ジェネレーター（`yield`・`toList`・遅延と `take`）| `13-continuation-monad.md` |

**未出**（16 回で初出になるもの）

- `Free` / `Pure` / `liftF` / インタプリタという語
- 命令をデータとして表す（DSL）という発想
- `DeriveFunctor`（シリーズ初の「便利のための言語拡張」）
- 「自由生成」（free construction）という語
- `free` パッケージ（シリーズ初の base・mtl 以外の依存）

## 既存記事との関係

**要調査**（TODO）。現時点で分かっているものだけ挙げる。

- **13 回**（[継続モナド 超入門](13-continuation-monad.md)）— ジェネレーターを `Cont` で
  実装している。**本文の題材が直接対応する**ので、`toList` の実装を並べて対比できる。
- Zenn の単発記事
  [継続モナドによるジェネレーターを Haskell で書く](https://zenn.dev/7shi/articles/20260730-haskell-generator)
  — 「継続をデータに格納する」点が構成案 2 と発想が近い。リンクするか要検討。
- `articles/haskell/haskell-experiments.md`（実験メモの一覧）には**追加しない**
  （13 回決定事項 8・14・15 回と同じ理由）。
- `articles/haskell/` の実験メモに Free モナドに触れたものがあるか要確認。
- 外部記事の棚卸しも要 TODO（Free モナドは日本語の解説記事が多く、
  シリーズの立ち位置（原理を自分で組む・圏論に寄せない）を明確にする必要がある）。

## TODO

- [x] **`stack` を修復する。**（2026-08-07 完了。haskell-* パッケージの更新で解消）
- [x] `stack` で `free` パッケージを導入し、検証環境を整える（決定事項 5）。
      `stack script` ヘッダ方式（resolver `lts-22.28`）で決定。`check/16-free/test.hs` で確認済み
- [ ] `free` の `Free`・`liftF`・`foldFree`・`iterM` を調べ、本文で示す自作定義との
      差分を対応表にまとめる
- [ ] `Free` を自作したコードを GHC 9.6.6 で動作確認する → `check/16-free/`
- [ ] `Free Two` が 15 回の `Tree` と同じ結果になることを確認する（構成案 1・問1）
- [ ] ジェネレーター DSL（`GenF`・`yield`・`toList`・`IO` 版）を実装する → `check/16-gen/`
- [ ] 13 回の `Cont` 版 `toList` と Free 版を並べて、対比の記述の裏を取る
- [ ] 無限の手順書に `take` が効くことを確認する（13 回の `nats` と同じ形）
- [ ] `DeriveFunctor` の有無で生成される `fmap` が手書きと一致するか確認する
- [ ] **手順書を `deriving Show` で覗けるか確認する。** `GenF` は関数を持たないので
      原理的には可能だが、`Free f a` の `Show` は `Show (f (Free f a))` を要求するため
      言語拡張が要る可能性がある。**要るなら見せない**（決定事項 6 以外の拡張は出さない）
- [ ] テレタイプ DSL と純粋インタプリタを実装する → `check/16-teletype/`（問3・問4）
- [ ] 左結合の `>>=` が遅くなることの記述の裏を取る（`:::message` の数行分のみ）
- [ ] 15 回の該当箇所（`## Tree`・練習【問4】・`## 3 段まとめて書く定型`）を読み直し、
      回収の言い回しを揃える
- [ ] 14 回の `deriving` の節・06 回 `:217` を読み直し、`DeriveFunctor` の導入を接続する
- [ ] 既存記事（`articles/haskell/`）の棚卸し
- [ ] 練習 4 問を実装して動作確認する → `check/16-exercises/`
- [ ] 本文の初稿を書く
- [ ] `README.md` の目次・`PREFACES.md`・`ARTICLES.tsv` を更新する
- [ ] 各記事の目次にある「1.【予定】Haskell Freeモナド 超入門」をリンクに差し替える
      （01〜15 回すべて。15 回公開時と同じ作業）
- [ ] 推敲
- [ ] Zenn へ複製し `ZENN.tsv` に追記する

## 推敲時の観点

14・15 回の同名の節から引き継ぐ。

- 半角スペースのルール（CLAUDE.md）。数詞は「3 つ」「2 節」と空ける。
  過去記事のタイトルをリンクテキストにしている箇所は実際のタイトルなので書き換えない。
- 予告 → 回収の対応が取れているか。**特に 15 回の `Tree`・`Rose`。**
- 掲載コードと `check/16-*/` のファイルが一致しているか。実行結果・エラー・警告も
  実際の出力と一致しているか。
- 過去記事を「第N回」のような回数で参照していないか（README.md「スタイル」）。
- 他記事へのリンクが行末の `👉[省略タイトル](URL#アンカー)` 形式に揃っているか。
- **まとめ節に次回予告が入っていないか**（決定事項 12）。
- 圏論に踏み込んでいないか（決定事項 4）。「自由生成」の 1 行以外に踏み込んでいないか。
- **言語拡張が `DeriveFunctor` 以外に出ていないか**（決定事項 6）。
- **codensity・Church エンコードの名前が出ていないか**（決定事項 10）。
- **13 回の説明が 1〜2 段落を超えていないか**（決定事項 14）。
  13 回を読んでいない読者が置いていかれていないか。
- **「Free モナドは難しい」という前提で書いていないか。** 15 回で自作モナドまで
  済ませた読者にとって、`Free` の `>>=` は 2 行で書ける。**難物として扱わない。**

## 17 回以降への引き継ぎ

- **Operational モナド**（17 回予定）。[README.md](README.md)「構想」に整理済み。
  Free が命令を包む関手に `Functor` インスタンスを要求するのに対し、
  Operational は継続を明示するエンコーディング（`instr :>>= k`）で `Functor` を不要にする。
  **16 回で `instance Functor` を手で書かせておくと、17 回の動機が立つ。**
  特に練習【問3】のテレタイプ（`GetLine (String -> next)` の関数合成）が効く。
- **Eff モナド**（18 回予定）。オープンユニオンによる複数効果の合成。
  10 回のモナド変換子との対比が主題になる。
- **codensity モナド / Church エンコード**（13-PLAN「今回は入れなかった話」の 3、
  決定事項 10 で 16 回から除外）。左結合の `>>=` が遅い問題への対策。
- **Filinski『Representing Monads』**（13-PLAN の 2、15 回決定事項 12）。置き場所は未定。
- **`Alternative`・`Foldable`・`MonadFail`・`Traversable`**（15 回決定事項 15・
  16 回決定事項 13）。08 回 `:564`・12 回 `:1584`・09 回 `:851` の回収が宙に浮いたまま。
  どこで拾うか（独立した回にするか）は別途決める。

## 補足

- 検証コードは `series/haskell-intro/check/16-*/` に置く（CLAUDE.md の「検証コード」の方式）。
  各ディレクトリに `README.md` を入れ、確認内容と実行結果を記録する。
  **`free` パッケージを使うディレクトリは、その旨と stack での実行方法も `README.md` に書く**
  （シリーズ初の base・mtl 以外の依存になるため）。他の `check/*/` は
  `runghc` で動く前提なので、実行方法が違うことを明記しておく。
- このファイル（`16-PLAN.md`）は拡張子を除く部分が大文字・数字・ハイフンのみなので
  `ARTICLES.tsv` の収集対象から除外される。
