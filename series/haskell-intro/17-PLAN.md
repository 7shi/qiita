# Haskell Operationalモナド 超入門 プラン

記事番号は 17、ファイル名は `17-operational-monad.md`
（2026-08-08 に `17-operational.md` → `17-operational-monad.md` と改名。13〜16 回の
`-monad` 命名規則に揃えた。同日いったん `17-freer-monad.md` に再改名したが、
タイトル変更の取り消しに伴い同日中に `17-operational-monad.md` に戻した。
下記「タイトルの変更と取り消し（2026-08-08）」を参照）。

**状態: 初稿完成（2026-08-08）。** 元は 16-PLAN.md の「17 回以降への引き継ぎ」を
出発点にした下書き（2026-08-07）。

- **2026-08-08**: 16 回の公開完了を受けて確定した事実を反映（下記「16 回の確定事項」）。
  題材・GADTs の導入方法・自作の範囲・Freer の経緯の 4 点を決定（下記「決定事項」）。
  前提知識を出典つきで整理（下記「前提知識」）。
- **2026-08-08（初稿）**: 未決事項をすべて実測で確定させ（下記「初稿時の検証結果」）、
  初稿 `17-operational-monad.md` と検証コード `check/17-program`・`17-stack`・`17-package` を作成。
  残る作業は推敲と Zenn への公開。
- **2026-08-08（タイトル変更 → 取り消し）**: ユーザー指摘（現代では Operational は
  obsolete で Freer が後継）を受けていったんタイトルを「Haskell Freerモナド 超入門」に
  変更したが、生態系の調査（素の Freer を提供するライブラリは存在せず、パッケージで
  素直に使えるのは Operational のみ）を踏まえ、**同日中に取り消して Operational に
  戻した**。代わりに Freer の節を拡充し、歴史の後で Freer のアイデア（線形化）を
  コードで説明する構成にする。下記「タイトルの変更と取り消し（2026-08-08）」を参照。

### 次にやること（セッション再開時の入口）

**初稿は書けた。次は推敲の段階。**

1. **初稿を読み直して推敲する。** 決定事項 3 の代償として懸念していた
   「存在型の説明の重さ」は、説明を 1 段落に留めた形で回避した（下記「初稿時の検証結果」）。
   分量・読みやすさの確認が要る。
2. ~~**導入文（PREFACES.md 用）を確認する。**~~ → **完了（2026-08-08）。**
   PREFACES.md に 17 回の導入文を追記し、ユーザー確認 OK を得た。
3. Zenn 側でスラッグを決めて `url` を埋め、CLAUDE.md「Zenn への移植」の手順で公開する。
4. 公開後、シリーズ README.md の 17 回の「（Zenn で公開）」表記を 13〜16 回と同じ形にする。

## 16 回の確定事項（2026-08-08 反映）

16 回は**公開完了**（`16-free-monad.md`、16-PLAN.md の TODO は全て消化済み）。
17 回の前提になる部分を確定値として書き出しておく。

|項目|確定した内容|
|---|---|
|16 回の URL|`https://zenn.dev/7shi/articles/20260808-haskell-free-monad`（行末 👉リンクはこれを使う。省略タイトルは `Freeモナド`）|
|用語|**「インタープリター」**（長音付き）。16 回は見出し `# インタープリター` を含め全箇所この表記。**本 PLAN も本文もこれで統一する**|
|検証コード|`check/16-free`・`16-gen`・`16-stack`・`16-teletype` の 4 ディレクトリ|
|言語拡張|GHC2021 基準、掲載コードに pragma なし（下記「言語拡張の基準」）|
|実行環境|`stack script --resolver lts-22.28 --package free`、`--system-ghc` なし（下記「16 回で確定した執筆環境」）|
|テレタイプ|**本文の解説**（`# テレタイプ`）。`instance Functor TeletypeF` を手書き掲載|
|練習【問3】【問4】|**スタックマシン**（`check/16-stack/`）。`Push Int next`・`Pop (Int -> next)` と `deriving Functor` を読者に書かせた|
|まとめ|次回予告なし。**17 回の存在は目次の「【予定】Haskell Operationalモナド 超入門」にのみ現れる**|

⚠ **16 回の推敲でテレタイプと練習の位置が入れ替わっている**（16-PLAN 変更点 13）。
本 PLAN の旧記述はこれを反映していなかったので修正済み（下記「この回の一行の主張」）。

## 公開方針

[README.md](README.md) の「公開方針（13回以降）」に従う。13〜16 回と同じ扱い。

- 執筆はこの Qiita 側リポジトリ、公開は Zenn。`id`・`url` は空のままにし、Zenn のスラッグが
  決まった時点で `url` を埋める。
- 補足・折りたたみは Zenn 記法（`:::message`・`:::details`）。
- 練習問題は解答を別記事にせず `:::details` で本文に統合し、関連する節の末尾に分散。
- 検証コードは `check/17-*/` に置き、各ディレクトリに `README.md`。
  `operational` パッケージは base・mtl 外の依存なので、stack での実行方法を明記する
  （16 回の `free` と同じ扱い）。

### 16 回で確定した執筆環境（そのまま踏襲する）

- **`stack script --resolver lts-22.28 --package <pkg> ファイル名.hs`** で実行する。
  掲載コードには `stack script` ヘッダを書かず、実行コマンド側で `--resolver`・`--package`
  を指定する（16-PLAN「free パッケージの扱い」）。記事に載せる resolver は執筆時点で
  最新 LTS を確認し直す。
- **`--system-ghc` は付けない。** 付けると `Setup.hs` のビルドが
  「files missing in the `base-4.18.2.1` package」で失敗する（16-PLAN 変更点 5）。
- 記事に載せる導入案内は 16 回の `free` の `:::message` と同じ形にする。

### 言語拡張の基準（GHC2021）

16 回で **GHC2021 を基準に変更し、掲載コードから `{-# LANGUAGE #-}` を全廃した**
（16-PLAN 変更点 11）。Haskell2010 で必要になる拡張は該当箇所に `:::message` で補足する。

- ⚠ **GADTs は GHC2021 に含まれないので、17 回では pragma が掲載コードに復活する。**
  16-PLAN 変更点 11 が「17 回の GADTs も同様」と明記済みで、既定路線。
  `DeriveFunctor`（GHC2021 で既定有効、16 回では pragma 不要）とは扱いが違う。
- この違いは**そのまま 17 回の説明に使える。** 16 回の `DeriveFunctor` は
  「便利のための言語拡張」で書かなくても済んだが、GADTs は
  「表現力のための言語拡張」なので明示が要る、という対比（構成案 3）。
- `ExistentialQuantification`・`RankNTypes` など併用が要るものが出たら、
  16 回と同じく `runghc -XHaskell2010` に掛けて洗い出し、`check/17-*/README.md` に表で残す。

## 記事の狙い（案）

**ゴールは「`Functor` を経由しないエンコーディング」。**

16 回の Free モナドは、`>>=` が枝を `fmap` で辿るため、命令の型に `Functor`
インスタンスを要求した。Operational は続き（継続）を明示的に持つ形にすることで、
`>>=` が `fmap` を使わずに済み、`Functor` インスタンスが不要になる。

同じ「DSL を組み立てて後から解釈する」発想のまま、エンコーディングだけを差し替える回。
**組み立てと解釈の分離という主題は 16 回から変えない。** 変わるのは表現の仕方だけ。

### この回の一行の主張（案）

16 回で手で書いた `instance Functor` が、まるごと要らなくなる。

|16 回（Free）|17 回（Operational）|
|---|---|
|`data GenF o next = Yield o next deriving Functor`|`data GenI o a where Yield :: o -> GenI o ()`|
|`data TeletypeF next = PutLine String next \| GetLine (String -> next)`|`PutLine :: String -> TeletypeI ()` / `GetLine :: TeletypeI String`|
|続きは `next` の位置に埋め込む|続きは `>>=` 側が持つ|
|`GetLine` の続きが関数になるのを自分で捌く|命令の戻り値の型を書くだけ|

特に **16 回の「続きが関数になる」命令が効く。** Operational でそれが消えるのを見せると
差分がはっきりする。16 回での扱いは次の 2 か所（**推敲時の変更点 13 で配置が変わった**ので、
出典を間違えないこと）。

|16 回での位置|内容|17 回での使い道|
|---|---|---|
|本文 `# テレタイプ`|`GetLine (String -> next)` と `instance Functor TeletypeF` を**手書きで掲載**。`fmap f (GetLine k) = GetLine (f . k)` が関数合成になることまで説明済み|`Functor` インスタンスが丸ごと消えることの比較対象|
|練習【問3】【問4】（スタックマシン、`check/16-stack/`）|`Pop (Int -> next)` と `deriving Functor` を**読者自身に書かせた**|同じ題材を Operational で書き直すと、手を動かした分だけ差分が体感できる|

⚠ 当初この欄は「16 回の練習【問3】のテレタイプ」と書いていたが、**16 回の推敲でテレタイプは
本文の解説に移り、練習はスタックマシンに差し替わった**（16-PLAN 変更点 13）。手で書かせた
`Functor` インスタンスはスタックマシンの側にある。

**16 回本文の次の一文が、そのまま 17 回の前振りになっている**（16-free-monad.md `# テレタイプ`）。

> 続きが値の命令では置いた値が結果になり、続きが関数の命令では置いた関数の戻り値が結果になる

Operational ではこれが「命令の戻り値の型を GADTs で宣言する」ことに置き換わる。
構成案 3 でこの一文を引き取ると接続が滑らかになる。

## Freer との関係・実用頻度（2026-08-07 追記）

**Operational と Freer は同じエンコーディングの別名。** タイトルは
「Haskell Operationalモナド 超入門」のまま（2026-08-08 にいったん Freer へ変更したが、
同日中に取り消して戻した。下記「タイトルの変更と取り消し（2026-08-08）」）。

```haskell
-- operational の Program
instr b :>>= (b -> Program instr a)

-- Freer
data Freer f a = Pure a | forall b. Impure (f b) (b -> Freer f a)
```

命令 1 つと「その結果を受け取って続きを返す関数」の組という、同じ形。

|名前|出自|由来|
|---|---|---|
|Operational|Apfelmus、`operational` パッケージと The Monad.Reader の記事（2010 年頃）|操作的意味論（operational semantics）|
|Freer|Kiselyov & Ishii "Freer Monads, More Extensible Effects"（2015）|`Free` より要求が少ない（`Functor` すら不要）＝「より自由」|

**Operational が先。** Kiselyov らが Extensible Effects を作り直す際に同じ形へ辿り着き、
`Free` との対比で `Freer` と命名した、という順序。
（2015 年の論文が Apfelmus を明示的に引用しているかは**未確認**。出すなら裏を取る。）

紛らわしい点として、**Extensible Effects の最初の版（2013 年、Kiselyov, Sabry, Swords）は
Free ベースで `Functor` を要求していた。** 2015 年の Freer 版がそれを作り直したもので、
現行の Eff 系ライブラリは後者。18 回で経緯に触れるならこの区別が要る。

- **決定: 本文で Freer の名前を出す。**（下記 TODO の「名前だけ出すか」は出す方向で確定）
  18 回で読者が `freer-simple`・`polysemy` などの外部資料に当たると Freer という語に
  出会うため。「このエンコーディングは Freer とも呼ばれ、Eff 系のライブラリはその名前を使う」
  という一言を置く。深入りはしない。
  **→ 2026-08-08 の方針転換で、`## Freer モナド` の節を拡充し、線形化（キュー）を
  コードで説明することになった**（次項）。

### タイトルの変更と取り消し（2026-08-08）

**いったん「Haskell Freerモナド 超入門」に変更したが、同日中に取り消して
「Haskell Operationalモナド 超入門」に戻した。** 記事本体・README.md・ARTICLES.tsv は
HEAD に restore 済み（変更はすべて未コミットだった）。この節には調査で得た
検証済みの事実を残し、冒頭に往復の経緯を記録する。

- **変更のきっかけ（ユーザー指摘）**: 「現代では Operational モナドは obsolete で
  Freer モナドが後継」。Eff 系ライブラリ（freer-simple・polysemy など）や日本語資料が
  Freer の名を使う以上、読者が後で出会う名前をタイトルに据える方がよい、という考え。
- **取り消しの理由（ユーザー判断、下記の調査結果を踏まえて）**: 素の Freer を
  提供するライブラリは存在せず、「Freer」を名指しで学べる実体は自作か
  Eff ライブラリのモジュール名しかない。**パッケージとして素直に使えるのは
  Operational だけ**なので、導入は Operational で行い、歴史を述べた後で
  Freer のアイデアをコードで説明する流れがよい、という結論。
- **実装差分の確認（ユーザー指示どおり事前に実施）**: 素の Freer（論文に倣った表記）は
  `Pure`/`Impure` が `Return`/`:>>=` に対応するだけで、自作 `Program` と構造は同一。
  **差分はコンストラクターの名前だけ**（GitHub 上の各種 Freer 実装でも
  `Pure`/`Impure` の表記を確認）。
- **新しい構成（ユーザー指示 2026-08-08）**: 「パッケージで素直に使える Operational で
  導入して、最後に歴史を述べた後で Freer について**線形化などコードでアイデアを説明**
  する」。`## Freer モナド` の節を拡充し、論文の `Pure`/`Impure` との対応と、
  続きをキューで保持する線形化（`FTCQueue`）をコードで示す。
  **キューは Freer の実装アイデアとして 17 回で扱い**、オープンユニオン（Eff）だけを
  18 回に残す（以前の「キューは 18 回に回す」方針から変更）。
  **節ごと `# 「Operational」とは何か` を `# operational パッケージ`
  （`## 性能の注意`）の後に移動**し、「性能問題の提示 → 歴史 → 線形化による解決」
  を隣接させた（「最後に歴史を述べた後で」という指示の反映。後方参照
  「性能の注意で見たように」が可能になった）。
- **パッケージの事情（実測、2026-08-08 に stackage で確認）**: `freer`・`freer-simple`
  は lts-22.28 に**未収録**（polysemy・effectful・fused-effects・cleff は収録）。
  素のエンコーディングをスナップショット内で提供するのは `operational` のみ。
  17 回の実行は `operational` で完結しており、`freer` パッケージは Hackage への
  参照リンクのみで実行には使わない。ただしリンク先の `freer` はオープンユニオン型の
  Eff 実装で、素の Freer 型を提供するわけではない点には留意。
- **18 回（Eff）の執筆環境（実測、2026-08-08）**: freer-simple-1.2.1.2 は
  **lts-23 系列にのみ収録**（lts-22・lts-24・nightly にはない）。18 回は
  `stack script --resolver lts-23.28 --package freer-simple` が使える。
  Freer ベースでモナド名も `Eff` なので、17 回からの物語にそのまま載る最有力候補。
  lts-22.28 に留まる場合の選択肢は polysemy（Freer ベースだが TH・GHC プラグイン前提で
  入門には重い）。effectful・cleff は `ReaderT IO` 方式、fused-effects は
  carrier class 方式で、いずれも Freer 系譜ではなく 17 回からの接続が切れる。
  resolver は記事ごとに明示する運用（16-PLAN「free パッケージの扱い」）なので、
  17 回 = lts-22.28、18 回 = lts-23.28 で問題ない。
  lts-23.28 には `operational`・`free` も収録済み。
  **「Freer」の名前はモジュール名（`Control.Monad.Freer`）にのみ残る**——doc-index に
  `Freer` 型や `Pure`/`Impure` のエントリはなく、コンストラクター `Val`/`E` は
  `Control.Monad.Freer.Internal` 行き（2026-08-08 に stackage のドキュメントで確認）。
  18 回は `import Control.Monad.Freer` することになるので、モジュール名が
  17 回の Freer の節との接続点になる。
- **freer-simple の実装との差分（freer-simple-1.2.1.2 のソースで確認、2026-08-08）**:
  ライブラリの `Eff` は素の Freer から 2 点拡張されている。
  1. **続きを素の関数ではなく型整列キュー（`FTCQueue`）で保持**
     （`data Eff effs a = Val a | forall b. E (Union effs b) (FTCQueue (Eff effs) b a)`）。
     `E u q >>= k = E u (q |> k)` とキュー末尾への追加（償却 O(1)）になるため、
     **17 回 `## 性能の注意` の左結合 `>>=` 二乗問題が起きない**。
     `send t = E (inj t) (tsingleton Val)` は `singleton i = i :>>= Return` に対応。
  2. **命令の型が単一の `instr` ではなくオープンユニオン**（`Union effs`、
     型レベルリスト + `Member` 制約）—— 18 回の主題（複数の効果の合成）。
  なお **freer-simple は素の `Freer` 型を公開していない**（`Eff` 一直線）。
  素の Freer（`Pure`/`Impure`）は論文の表記としてのみ存在する。
  **キュー（線形化）は 17 回の `## Freer モナド` でコードを示して扱い、
  オープンユニオン（Eff）のみ 18 回に残す**（「タイトルの変更と取り消し」の
  新しい構成を参照。以前は「キューは 18 回に回す」方針だったが転換した）。
- **（参考）operational パッケージの内部表現（0.2.4.2 のソースで確認）**: 実際の
  `ProgramT` は `Lift`/`Bind`/`Instr` の 3 コンストラクターで、`viewT` が
  `Return`/`:>>=` の形（`ProgramViewT`）に 1 段ずつ整えて見せる。
  本文の「`view` で 1 段だけ剥がす」という説明と齟齬なし。
- **変更箇所（いずれも取り消し済み）**: フロントマターの `title`・タグ、ファイル名、
  冒頭段落、命名の節、まとめ冒頭、シリーズ README.md、ARTICLES.tsv を変更したが、
  取り消しに伴いすべて HEAD に restore した。**残る差分はこの PLAN と、
  `## Freer モナド` 節の拡充（線形化のコード）、README.md「構想」の
  ギャップ記録のみ。**
- **過去記事（01〜16）の目次は結果的に無関係。** タイトルが Operational に戻ったため、
  「【予定】Haskell Operationalモナド 超入門」行は正しいまま。
- **本文中の「Operational」の呼称は全面維持。** タイトルも Operational に戻ったため、
  橋渡しの措置は不要になった。代わりに `## Freer モナド` 節を拡充する（上記の新しい構成）。

### 外部記事との関係（2026-08-08 追記）

16 回の推敲で日本語の Free モナド解説を棚卸しした（16-PLAN「既存記事との関係」）。
そこで**この回と最も近い日本語資料**が見つかったので記録しておく。

- [Freer Effectsが、だいたいわかった](https://qiita.com/YoshikuniJujo/items/988ac4b69a27974154fd)
  （YoshikuniJujo、導入 + 全 16 回のシリーズ）
  - 圏論用語を避けて自作していく方針が本シリーズと同じ。**Free の概要から入り、
    存在型・型族・GADTs を個別に解説してから Freer と Extensible Effects へ進む**という
    構成も、16 → 17 → 18 回の流れとほぼ重なる。
  - **第 8 回は「Freerモナド(Operationalモナド)」と両名を併記**し、
    `data Freer t a = Pure a | forall x. Bind (t x) (x -> Freer t a)` を
    **パッケージを使わず自作**している（2026-08-08 に直接確認）。
    導出経路は **Free + Coyoneda の融合**（Coyoneda を型シノニム化して Free に
    埋め込む）で、17 回の「続きを外に出す」直接導入とは別経路。
    第 15 回「関数を保管しておくデータ構造による効率化」でキュー（FTCQueue 相当）
    まで自作している。タイトル変更後の 17 回の名前の扱い（Freer と Operational の
    併記）とも一致する。
  - **17 回の範囲では衝突しない。** あちらは Freer Effects がゴールで、
    こちらは「エンコーディングの差し替え」だけを 1 回分に切り出す構成のため。
    ただし**18 回（Eff）では正面から重なる**ので、その時点で改めて棚卸しする。
  - GADTs・存在型の導入の仕方（上記 TODO）で迷ったときの比較対象になる。
    **参考にはするが、本文からリンクは張らない**（16 回と同じ方針。読者を別の前提へ
    送らない）。
- 「Operational」という名前で書かれた日本語の解説は、Free に比べて格段に少ない。
  この回の存在意義（Eff の前段として独立させる）はその点でも立つ。

#### 「Freer モナド」を扱う記事のパッケージ調査（2026-08-08、Web 検索 + 直接確認）

- **パッケージを使う記事は Eff 系一直線**: freer-effects・freer-simple・polysemy。
  いずれもオープンユニオン込みの `Eff` で、素の Freer 単体を扱う記事は見つからない。
- **素の Freer は自作する記事が多い**: 上記 YoshikuniJujo シリーズのほか、
  GitHub 上にも `data Freer f a where Pure …; Impure …` の自前実装が多数ある。
- **タイトル変更の裏付け**: 「Freer モナド」を学ぶ経路は「自作するか、
  Eff ライブラリ（のモジュール名 `Control.Monad.Freer`）の中で出会うか」の
  2 つしかなく、17 回の構成（自作 `Program` で導入、タイトルは Freer）は
  この生態系と一致する。
- ⚠ Web 検索の AI 要約は YoshikuniJujo シリーズを「freer-effects パッケージ使用」と
  **誤答**していた。直接確認で訂正した経緯を残す（本シリーズの裏取り方針どおり）。

### 実用頻度についての注意

**`operational` パッケージ自体の実務での採用例は稀。** 教材としての性格が強い。
一方で**エンコーディングは現代のエフェクトシステムの土台**になっている。

|パッケージ|立ち位置|
|---|---|
|`free`|依存としては多い。ただし「Free で DSL を書く」用途は限定的|
|`operational`|ほぼ教材。実務での採用例は稀|
|`polysemy`・`effectful`・`freer-simple`・`fused-effects`|エフェクトシステムとして実際に使われている|
|`mtl`|依然として主流|

※ Hackage のダウンロード数などは**未確認**。記事に定量的な主張を書くなら裏を取る。

記事では「パッケージは広く使われていないが、この形が Eff 系の土台になっている」という
位置づけを明示する。**実用頻度を偽らない。** 逆に言えば、17 回の価値は
「18 回（Eff）でエンコーディングの転回とオープンユニオンを同時に教えずに済む」ことにある。
オープンユニオンだけでも `DataKinds`・`TypeOperators`・型レベルリスト・`Member` 制約と重いので、
負荷分散として 17 回を分ける意義がある。

## 16 回からの引き継ぎ（回収候補）

16-PLAN.md「17 回以降への引き継ぎ」より。**すべてを 17 回で回収する必要はない。**

|項目|17 回で扱うか|メモ|
|---|---|---|
|Operational モナド|**主題**|—|
|`Foldable`・`Traversable`|**扱わない（決定）**|下記。対比が綺麗に立たなかった|
|codensity・Church エンコード|保留|左結合 `>>=` の対策。18 回以降でも可|
|`Alternative`・`MonadFail`|保留|Operational と関係が薄い|
|Filinski『Representing Monads』|**扱わない（決定）**|README の「未定のアイデア」＞「制御構造をモナドで書くという理論」へ集約した|

### `Foldable`・`Traversable` の扱い（検討して見送りに決定）

16 回では決定事項 13 で「扱わない」とした。17 回で「Free はなれるが Operational はなれない」
という**対比として**拾えないか検討したが、**成り立たなかったので見送る。**

検証結果（`ghc` / `stack script --resolver lts-22.28 --package free` で確認）。

|型|`Foldable`|根拠|
|---|---|---|
|`Free Two a`（木）|**可**|`sum` → `13`、`foldr (:) []` → `[1,10,2]`、`traverse` も通る|
|`Free (GenF o) a`（`Yield o next`）|**可**|ただし集まるのは最後の戻り値だけで無意味|
|`Free TeletypeF a`（`GetLine (String -> next)`）|**不可**|下記エラー|
|Operational / Freer|**不可**|続きが常に関数の中にある|

```text
error: [GHC-16437]
    • Can't make a derived instance of ‘Foldable TeletypeF’:
        Constructor ‘GetLine’ must not contain function types
```

**真の境界は「Free か Operational か」ではなく「続きが関数の中にあるかどうか」で、
これは Free の内側を貫いている。** 16 回のテレタイプは Free でありながら `Foldable` に
できない。Operational は構造上つねに続きが関数なので決して満たさない、という言い方なら
正確だが、**説明するのに 3 ケース要る。** 17 回の主題に対して寄り道。

見送りの理由:

1. **対比が綺麗に立たない**（上記）。無理に立てると不正確になる。
2. **17 回は既に重い。** GADTs／存在型というシリーズ初の「表現力のための言語拡張」を
   背負う回に、未紹介の型クラスを 2 つ足すのは過積載。
3. **本来の回収先が違う。** `Foldable`・`Traversable` は 15 回（`Functor`・`Applicative`・
   `Monad` の 3 段）の穴なので、その続編で扱うのが筋（README.md「構想」の「網羅性の穴」）。

### 型クラス名を出さずに使える洞察（採用）

上記の裏にある事実は、**型クラス名を出さずに 1 段落で使う。** Operational の位置づけを
支えるので、名前なしなら置く価値がある。文案:

> Operational では、続きが関数の中にあります。つまり入力を与えないと先へ進めないので、
> 手順書の中身を外から並べて取り出すことはできません。**インタープリターを通す以外に中を見る
> 方法がない**、という構造になっています。

「Free は汎用のデータ構造でもあり、Operational は DSL 専用」という整理につながる。
置き場所は構成案 6（Free と Operational の使い分け）が候補。

## 構成案（ざっくり）

1. **Free の復習と不満** — 16 回の `GenF`・`TeletypeF` を再掲し、`Functor` インスタンスが
   要ること、特に `GetLine` で続きが関数になることを思い出させる。
   16 回を読んでいない読者にも分かるよう独立して説明し直す（README のスタイル）。
   16 回本文で `instance Functor TeletypeF` を手書き掲載してあるので、そこから引ける。
2. **続きを外に出す** — `Yield o next` の `next` を型から外し、代わりに `>>=` の側が
   `(b -> Program instr a)` として持つ形へ。ここが転回点。
   `>>=` が継続を合成するだけになり、`fmap` が消えることを見せる。
3. **GADTs で命令を並べる** — 命令の戻り値の型を各コンストラクターが宣言する。
   `Yield :: o -> GenI o ()`、`GetLine :: TeletypeI String` の形。
   **シリーズ初の「表現力のための言語拡張」**（16 回の `DeriveFunctor` は
   「便利のための言語拡張」だった。この対比は使えそう）。
   - **まず通常の `data` で書いてみて、行き詰まりを見せてから GADT 構文を出す**
     （決定事項 2）。

     ```hs
     -- まず普通に書いてみる
     data TeletypeI a
         = PutLine String
         | GetLine
     -- → a がどこにも現れない。
     --   GetLine の結果が String だと型に書けない

     -- そこで GADT 構文
     data TeletypeI a where
         PutLine :: String -> TeletypeI ()
         GetLine ::           TeletypeI String
     ```

   - **GHC2021 に含まれないので pragma が要る**という違いが、この対比を裏付ける
     （上記「言語拡張の基準（GHC2021）」）。16 回は掲載コードから pragma を全廃したので、
     17 回で pragma が復活すること自体が「今までとは違う種類の拡張だ」という合図になる。
   - 16 回本文の「続きが値の命令では置いた値が結果、続きが関数の命令では置いた関数の
     戻り値が結果」を引き取り、**それが命令の型の宣言そのものになる**と繋げる。
4. **インタープリター** — `view` で 1 段ずつ剥がして辿る。16 回と同じ題材
   （ジェネレーター・テレタイプ）を同じインタープリターの形で書き直し、**手順書の表現が
   変わってもインタープリターの書き方は変わらない**ことを見せる。
5. **operational パッケージ** — `Program`・`singleton`・`view`・`ProgramView`。
   16 回の `free` パッケージ節と同じ位置づけ。**自作した `Program` の後に置く**
   （決定事項 3）。
6. **Free と Operational の使い分け** — 表で整理。
7. **まとめ** — 次回予告は書かない（16 回決定事項 12・ユーザー指示）。

**練習はスタックマシン**（決定事項 1）。16 回の練習【問3】【問4】（`check/16-stack/`）を
Operational で書き直す形。`Pop (Int -> next)` + `deriving Functor` が
`Pop :: StackI Int` になり、**読者が 16 回で自分の手で書いた `Functor` インスタンスが
消える**という体験になる。置き場所は構成案 3・4 の末尾（16 回と同じく節末に分散）。

## 決定事項

**覆す場合は理由を確認してから。** 1〜4 は 2026-08-08 に決定。

1. **題材は 16 回と同じ配分で書き直す。** 本文はジェネレーター（転回点の導入）→
   テレタイプ（続きが関数の消失・GADTs の戻り値の型が複数出る）、練習はスタックマシン。
   16 回と一対一で対応するので差分が最も見えやすい。
   - ⚠ **リスクは単調さ。** 3 題材すべてが 2 回連続で登場する。
     「同じものをもう一度作る」のではなく**「同じものが違う書き方でどうなるか」**を
     毎回明示して、再掲に見えないようにする。
   - 新規題材は足さない。GADTs の戻り値の型が複数あることはテレタイプ
     （`PutLine :: () / GetLine :: String`）で十分示せる。
2. **GADTs は「通常の `data` で書いてみて行き詰まる」→「GADT 構文」の順で導入する。**
   `data TeletypeI a = PutLine String | GetLine` では型引数 `a` がどこにも現れず、
   `GetLine` の結果が `String` であることを型に書けない。この行き詰まりを見せてから
   GADT 構文を出す（構成案 3 にコード案）。
   - 16 回の `DeriveFunctor`（手書き `instance Functor` → `deriving`）と**同じ型の導入**。
     「まず手で書いてみる → 書けない／面倒だと分かる → 拡張を出す」という運び。
   - **存在型・型の等式といった理屈には立ち入らない。** 説明は
     「各コンストラクターが戻り値の型を自分で宣言できる」の一点に絞る。
     ただし自作 `Program`（決定事項 3）で `forall b.` が出るため、
     そこで最小限の言及が要るかは執筆時に判断する。
3. **`Program` を完全に自作してから `operational` パッケージへ進む。** 16 回
   （`Free` を自作 → `free` パッケージ）と同じ流れ。`data Program`・`instance Monad`・
   `view` を自分で書く。
   - 仕組みが手の内に入るので「なぜ `Functor` が要らないのか」が実感できる。
   - **代償は `forall b.`（存在型）が避けられないこと。** 決定事項 2 と合わせて
     重くなりすぎないか、初稿を書いた時点で分量を見る。
4. **Extensible Effects の 2013 年版／2015 年版の経緯は 18 回に回す。**
   17 回では「このエンコーディングは Freer とも呼ばれ、Eff 系のライブラリはその名前を使う」
   の一言に留める（上記「Freer との関係」の決定を維持）。
   17 回の主題はエンコーディングの差し替えであって、ライブラリの歴史ではないため。
   ※ 2026-08-08 のタイトル変更に伴い、Freer の説明は「一言」から `# 「Freer」とは何か` の
   主役へ格上げされた（`Pure`/`Impure` との対応を明示）。2013/2015 年の経緯を
   18 回に回す方針自体は変わらない。

## 前提知識

**16 回とは事情が違う。** 16 回は「15 回までで揃っている。新規に前提となる材料はない」と
書けたが、**17 回は未出のものを 4 つ背負う**（下記）。決定事項 2（GADTs の段階的導入）と
決定事項 3（`Program` の完全自作）はどちらも未出を増やす方向の決定なので、
**この節が過積載の点検そのものになる。**

### 既習の材料

**執筆時は必ず各箇所を開いて、当時の言い回しに繋がる形で書くこと。**

|材料|出典|17 回での使いどころ|
|---|---|---|
|`data` / 再帰的なデータ型 / 型引数|`02-algebraic-data-types.md` `# 直和型`(`:278`)|命令の型を宣言する土台|
|`{-# LANGUAGE #-}` の書き方|`06-io-monad.md:217`（`UnboxedTuples`）・`09-state-monads.md:237`|**GADTs で pragma が復活する**（上記「言語拡張の基準」）|
|`class` / `instance`|`14-type-classes.md` `# class と instance`(`:94`)|`instance Monad (Program instr)` を書く|
|種（`:k`）|〃 `# 型引数を取る型クラス`(`:819`、`:k` の実行例は `:860` 以降)|`instr` の種が `* -> *` であること|
|`deriving`|〃 `# deriving`(`:296`)|**16 回の `deriving Functor` が消えることが主題**|
|`Functor` / `Applicative` / `Monad` の 3 段|`15-monads-and-friends.md`|`Program` を 3 段揃える|
|定型（`fmap = liftM` / `(<*>) = ap`）|〃 `## 3 段まとめて書く定型`(`:1012`)|自作 `Program` でそのまま使える|
|モナド則|〃 `## モナド則`(`:672`)|`>>=` が命令をつなぐだけであることの確認|
|`Free` / `Pure` / `liftF`|`16-free-monad.md` `# 木構造を一般化する`(`:53`)・`## liftF`(`:375`)|**対比の起点**|
|命令の型・手順書・DSL という語|〃 `# 手順書を組み立てる`(`:319`)|そのまま引き継ぐ|
|スマートコンストラクター|〃 `## liftF`(`:396` で定義)・`## スマートコンストラクター`(`:615`)|`singleton` で書き直す|
|インタープリター|〃 `# インタープリター`(`:445`)|**用語は長音付きで統一**（上記「16 回の確定事項」）|
|続きが関数になる命令|〃 `# テレタイプ`(`:593`)・練習【問3】|**Operational で消えるもの**|
|外部パッケージを stack で使う|〃 `# free パッケージ`(`:791`)|`operational` も同じ手順（上記「執筆環境」）|

**必須にはしないが効くもの**

- `13-continuation-monad.md` `## bind と CPS`(`:85`) — 「続きを関数として持ち回る」という
  見方。Operational の `instr b :>>= (b -> Program instr a)` はまさにこの形なので、
  13 回を読んでいれば腑に落ちやすい。ただし**必須にはしない**。
  13 回を読んでいない読者にも独立して分かるように書く（README のスタイル）。
- `14-type-classes.md` `# 辞書渡し`(`:926`) — `instance` が値として渡されるという見方。
  「`Functor` インスタンスが要らない」の意味を深く理解する助けにはなるが、
  **本文で辞書渡しに言及する必要はない。**

### 未出（17 回で初出になるもの）

|初出|重さ|扱い|
|---|---|---|
|GADTs（`{-# LANGUAGE GADTs #-}`）|**シリーズ初の「表現力のための言語拡張」**|決定事項 2。通常の `data` の行き詰まりから導入|
|存在型（`forall b.`）|理屈は重い|決定事項 3 で自作 `Program` に不可避。**説明は最小限に留める**（要検討）|
|`Program` / `singleton` / `view` / `ProgramView`|`operational` の API|構成案 5。API 確認は TODO|
|Freer という呼び名|軽い|一言のみ（決定事項 4）|

- `operational` パッケージ自体は「base・mtl 外の依存」として 16 回の `free` で経験済みなので、
  **導入手順は未出ではない。** 実行コマンドの形も確定している。
- ⚠ **重いものが 2 つ（GADTs・存在型）同時に来る。** 16 回の `DeriveFunctor` は
  1 つだけ、しかも「書かなくても済む便利」だった。ここが 17 回の最大の負荷。
  初稿の分量を見て、存在型の説明を削れるか（`view` をパッケージ任せにするなど）を
  判断する（決定事項 3 の代償）。

## 初稿時の検証結果（2026-08-08）

初稿作成時に実際に動かして確定した事実。**記憶ではなく実行結果に基づく。**

- **`operational-0.2.4.2` は lts-22.28 に収録されている。** resolver の変更は不要で、
  実行コマンドは 16 回と同じ形 `stack script --resolver lts-22.28 --package operational`
  で動いた（`check/17-package/`）。
- **API は実動確認済み。** `Program`・`singleton`・`view`・`ProgramView`
  （`Return`・`:>>=`）・`interpretWithMonad` が本文の記述どおり動く。
  - **`view` の戻り値 `ProgramView` は `Show` を持たない**（`print (view prog)` は
    型エラー）。本文では触れていないが、検証で判明した事実として
    `check/17-package/README.md` に記録。
- **スタックマシンは `Pop :: StackI Int` で書けた**（`check/17-stack/`）。
  16 回の `Pop (Int -> next)` + `deriving Functor` がまるごと消えることを確認。
- **左結合 `>>=` は Operational でも二乗のオーダー**（`check/17-program/Slow.hs`）。
  N を 2 倍にすると約 4 倍（0.026→0.066→0.246→1.026 s）。理由は
  `(i :>>= j) >>= k = i :>>= (\b -> j b >>= k)` が左側を 1 段ずつ剥がすため。
  **16 回と同じく本文には数値を載せず、現象と理由だけ**を書いた。
  codensity・Church エンコードは**17 回でも名前を出さなかった**（16 回決定事項 10 を踏襲）。
- **言語拡張は GADTs のみ**で足りる。`runghc -XHaskell2010` で確認すると
  「Illegal generalised algebraic data declaration」が唯一のエラー。
  `:>>=` の `b`（存在型に相当）は GADT 構文の中で完結し、
  `ExistentialQuantification` は不要だった。**これにより決定事項 3 の代償
  （存在型の説明）は、「`b` は手順書全体の型に現れない」1 段落の言及で済んだ。**
- **「Operational」の由来は裏取り済み。** Heinrich Apfelmus による
  The Monad.Reader Issue 15（2010 年）のチュートリアル "The Operational Monad Tutorial"
  と `operational` パッケージ。操作的意味論（operational semantics）に由来する。
  本文ではこの範囲で触れた（`# 「Operational」とは何か`）。

### 初稿の構成（構成案との対応）

構成案どおり。節見出しは次のとおり。

1. `# Free モナドの復習` — 16 回の `Free`・`GenF`・`TeletypeF`・手書き `instance Functor`
   を再掲し、不満（`Functor` が要る・続きが関数の捌きが込み入る）を明示。
2. `# 続きを外に出す` — 自作 `Program`（GADT 構文）と `instance Monad`。
   Free の `>>=` と並べて `fmap` の消失を見せ、`singleton` を `liftF` に対応させた。
   13 回の CPS への 👉リンクは「効くが必須にしない」の方針どおりさらりと。
3. `# GADTs で命令を並べる` — 通常の `data`（型引数 `a` が現れない）で行き詰まってから
   GADT 構文（決定事項 2）。pragma 復活を「便利のための拡張（16 回の `DeriveFunctor`）
   ではなく表現力のための拡張」という対比で説明。16 回の「置いた関数の戻り値が結果」を
   引き取って「命令の型の宣言そのもの」に繋げた。
4. `# インタープリター` — ジェネレーター（`toList`）とテレタイプ（`runPure`・`runIO`）。
   **16 回の解答と 1 行ずつ対応する形**に揃え、「表現が変わってもインタープリターの
   書き方は変わらない」を示した。末尾に練習【問1】【問2】（スタックマシン、`:::details`）。
5. `# 「Operational」とは何か` — 名前の由来（上記）。Freer の由来（より自由）を
   16 回の「自由」に接続。2013/2015 年の経緯は 18 回に回す決定どおり一言のみ。
6. `# operational パッケージ` — `view`/`ProgramView` と `interpretWithMonad`
   （`foldFree` に相当と明記）。stack の `:::message` は 16 回と同じ形。
   「パッケージ自体の実務採用は稀だが土台になっている」という位置づけも明記。
   `## 性能の注意` は 16 回と同じ通常小見出し（`:::message` 非使用も踏襲）。
7. `# Free と Operational の使い分け` — 表で整理。「型クラス名を出さずに使える洞察」
   （続きが関数の中にあり中を見る方法がない）を採用。ただし「Free でも続きが関数の
   命令は同じ制約を受ける」と**絶対的な違いではないことも明記**（対比の誇張を避ける）。
8. `# まとめ` — 対応表。次回予告なし。

### 初稿時の変更点・判断

- **冒頭の Claude Code 表記は実態に合わせて「GitHub Copilot CLI (Kimi K3)」とした。**
  Zenn 側の慣例（CLAUDE.md 手順 5）は Claude Code 前提の文面なので、
  公開時に Zenn 側へ複製する際の扱いは要確認。
- 練習の番号は 16 回の【問3】【問4】に対応するが、**17 回内では【問1】【問2】**とした
  （17 回はこの 2 問のみなので）。
- 本文中の「種」へのリンクは 16 回 `# 種`、CPS は 13 回 `## bind と CPS`、
  定型は 15 回 `## 3 段まとめて書く定型`、GHC への拡張指定は 9 回を 👉リンクで引いた
  （アンカーは `scripts/anchor.py` で生成）。
- `url` は Zenn スラッグ未定のため空のまま（公開方針どおり）。

## 未決事項・TODO

- [x] `operational` パッケージの API を実際に確認する → **確認済み（上記「初稿時の検証結果」）。
      lts-22.28 に収録、`Program`・`singleton`・`view`・`ProgramView`・
      `interpretWithMonad` すべて実動確認。**
- [x] 自作版をどこまで書くか → **完全に自作してからパッケージへ**（決定事項 3）。
      16 回と同じ流れ。存在型（`forall b.`）と GADTs で重くならないかは初稿の分量で判断。
- [x] **GADTs をどう導入するか** → **通常の `data` で行き詰まってから GADT 構文**
      （決定事項 2）。`ExistentialQuantification` との関係には立ち入らない。
- [x] Freer モナドという呼び名を出すか → **出す。** 上記「Freer との関係」を参照。
      同一のエンコーディングであることと、Eff 系が Freer と呼ぶことを一言。深入りはしない。
      **→ さらにタイトル自体を Freer に変更**（「タイトルの変更（2026-08-08）」）。
- [x] 2013 年版／2015 年版 Extensible Effects の経緯を 17 回で書くか 18 回に回すか
      → **18 回に回す**（決定事項 4）。17 回は「Freer とも呼ぶ」の一言のみ。
- [x] 左結合 `>>=` の性能問題は Operational でも残る（はず）→ **実測で確認。二乗のオーダー**
      （上記「初稿時の検証結果」）。16 回と同じ扱い（`## 性能の注意` の小見出し、
      数値は本文に載せず現象と理由だけ）で書いた。
      **codensity・Church エンコードは 17 回でも名前を出さなかった**（16 回決定事項 10 踏襲）。
- [x] 題材をどうするか → **16 回と同じ配分で書き直す**（決定事項 1）。
      本文はジェネレーター → テレタイプ、練習はスタックマシン。新規題材は足さない。
      - **スタックマシンは 16 回の練習【問3】【問4】**（16-PLAN 変更点 13、
        `check/16-stack/`）。`Pop (Int -> next)` + `deriving Functor` が要ったところを
        Operational では `Pop :: StackI Int` + GADTs で書いた（**検証済み**、
        `check/17-stack/`）。ジェネレーターを 13 回（継続）と 16 回（Free）で
        作り直したのと同じ構図。
      - 単調さへの対策は決定事項 1 の ⚠ を参照。
- [x] 前提知識の整理 → 上記「前提知識」節。既習の材料を出典つきで表にし、未出 4 つ
      （GADTs・存在型・`operational` の API・Freer）を洗い出した。
      13 回（継続）は効くが**必須にしない**、14 回の辞書渡しは**本文で触れない**と整理。
      - **初稿での実績**: 存在型は「`b` は手順書全体の型 `a` に現れない」1 段落の言及に
        留まり、理屈には立ち入らなかった（GADTs のみで足りたため。上記「初稿時の検証結果」）。
- [x] 導入文（PREFACES.md 用）の案を書く。**初稿を書き上げてから確認する**ので、
      事前に議論しない（2026-08-08 ユーザー指示）。**→ 完了（2026-08-08）。**
      PREFACES.md に 17 回の導入文を追記し、ユーザー確認 OK を得た。

## 検討メモ

- **Free との優劣を煽らない。** Operational が「上位互換」ではない。Free は再帰的な
  データ構造そのものなので木として扱いやすく（上記「型クラス名を出さずに使える洞察」）、
  Operational は命令の列挙が楽。用途が違うという整理にする。
- **圏論には言及しない**（README のスタイル）。Freer・Kan 拡張・codensity の圏論的背景には
  立ち入らない。名前を出すとしても由来として触れるに留める。
- 16 回の「自由」の節に相当する、名前の由来を説明する節を置くか。
  「Operational」は operational semantics（操作的意味論）から来ているはずだが、
  ~~未確認。出すなら裏を取る~~ → **裏取り済み。`# 「Operational」とは何か` として
  初稿に書いた**（上記「初稿時の検証結果」）。
  Freer の由来（`Free` より要求が少ないので「より自由」）と並べた構成で、
  16 回の「自由とは何か」への比較級としての接続もそのとおり行った。

## 補足

- 検証コードは `series/haskell-intro/check/17-*/` に置く（CLAUDE.md の「検証コード」の方式）。
- このファイルは拡張子を除く部分が大文字・数字・ハイフンのみなので
  `ARTICLES.tsv` の収集対象から除外される。
