# Haskell 圏論 超入門 プラン

記事番号は 20、ファイル名は `20-category-theory.md`（予定）。13〜18 回は `-monad`、
19 回は `-arrow` と、扱う概念の名前をそのまま取ってきた。今回も同じ。

**状態: 構想中（2026-08-13）。本文未着手。**

**20 回をシリーズの完結とする。** 末尾に補遺（扱えなかった話題の整理）と
全 20 回の総括を置く。19 回決定事項 3（締めくくりを置かない）はここで解除する。

16〜19 回と同じく、決着した判断は「決定事項」に置く。
**執筆・推敲時の判断で覆した点は「執筆時の変更点」に追記していく**（現時点では空）。

## 次にやること（セッション再開時の入口）

1. **検証コードを書く**（下記「検証項目」）。19 回と同じく、本文を書く前に
   実際に動かして事実を確定させる。特に言語拡張の要否は記憶で判断しない。
2. 過去記事の該当箇所を読み直す（下記「前提知識」の表。特に 15 回 `## ファンクター則`・
   `### >=> で書き直す`、16 回 `# 「自由」とは何か`、17 回 `## 継続を命令の型から外す`、
   19 回 `## Category`・`## Kleisli`）。
3. 番外編 `monad-laws.md`・`monad-laws-2.md` を読み、重複と接続を確認する。
4. 未決事項（下記）を潰す。
5. 初稿を書く。

## 記事の狙い

**ゴールは「これまで書いてきたコードに、後から名前を与える」。**

シリーズは 1〜19 回を通じて「圏論には言及しない」方針で書いてきた
（[README.md](README.md)「スタイル」。6〜10 回は導入文にも明記している）。
20 回はその方針を**明示的に解除する回**にあたる。

ただし**理論から Haskell を導く通常の向きにはしない。** シリーズで積み上げた
Haskell の道具立ての側から理論の概念を眺める、**逆向きの構成**にする
（README.md「未定のアイデア」の共通方針）。

### この回の一行の主張

**圏論の概念は、シリーズで既に実装として通ってきた。**

|シリーズで書いたコード|圏論の名前|出典|
|---|---|---|
|`class Category cat` の `id`・`.`|圏（対象・射・合成・単位元）|19 回|
|`(->)` のインスタンス|Hask 圏|19 回|
|`Kleisli m` のインスタンス|Kleisli 圏|19 回|
|`Functor` とファンクター則|（自己）関手|15 回|
|`f ~> g` の形のハンドラー・`foldFree`|自然変換|16・18 回|
|`>=>` が結合的で `return` が単位元|モノイド対象|15 回|
|`Free` の「モナド則だけを満たす」|自由生成（忘却関手の左随伴）|16 回|
|`instr :>>= k` に `Functor` が不要|Coyoneda（米田の補題）|17 回|

**この表がそのまま記事の目次になる。**

### 方針転換をどう書くか

冒頭で「圏論には言及しない」を引き取り、**なぜ今なら扱えるのか**を述べる。
19 回までの実装が前提として全部揃ったから、という理由づけ。

⚠ **前言撤回や謝罪にしない。** 順序の問題だったという立場で書く。
これは `# 総括` で回収する伏線になるので、**冒頭と総括を対にして書く。**

### 記事の導入文（案・PREFACES.md 用）

> Haskell の解説で圏論の名前を見かけることがありますが、このシリーズは
> 一貫して圏論に言及せずに進めてきました。最終回では順序を逆にして、
> これまで書いてきたコードの側から圏論の概念を眺めます。圏・関手・自然変換から
> 「モナドは自己関手の圏におけるモノイド対象」まで、既に実装として通ってきたものに
> 後から名前を与えていきます。

## 決定事項

**すべて 2026-08-13 のユーザー判断、またはシリーズの既定方針からの引き継ぎ。**

1. **逆向きの構成。** 各節は「シリーズで書いたコード」から始め、定義は後から与える。
   **抽象的な定義で節を開かない。**
2. **図式は最小限。** 可換図式を多用しない。等式と実行できるコードで示す。
   mermaid を使うとしても 1〜2 個（19 回の配線図と同程度）。→ 未決事項。
3. **base だけで完結させる**（19 回決定事項 12 と同じ）。`Coyoneda`・`Yoneda`・
   自然変換は kan-extensions パッケージにあるが**自作する。** パッケージ名は紹介に留める。
   読者は `runghc` だけで全部追える。
4. **言語拡張の基準は GHC2021**（README.md「スタイル」）。
   必要になりそうなのは `RankNTypes`・`TypeOperators`・`ExistentialQuantification`
   （いずれも GHC2021 に含まれる見込み ＝ プラグマ不要）と、`Coyoneda` を GADT 構文で
   書く場合の `GADTs`（含まれないのでプラグマが必要）。
   - ⚠ **記憶で判断せず `runghc -XHaskell2010` に掛けて確かめる。** 結果は
     `check/20-*/README.md` に表で残す。
5. **練習問題を出さない。** 13〜19 回はすべて練習つきだったが、20 回だけは置かない。
   **圏論の練習は Haskell を書く問題ではなく数学の問題になり**、シリーズの
   「書いて動かす」姿勢と噛み合わないため。**読み物と割り切る。**
   - ⚠ これに伴い `:::details` の用途がなくなる（README.md「公開方針」で
     解答例専用と定めているため）。**20 回に `:::details` は登場しない。**
   - 代わりに本文のコードは読者がそのまま動かせる形に揃える（下記「練習を出さないことの補償」）。
6. **20 回をシリーズの完結とする。** 19 回決定事項 3（シリーズ最終回としての
   締めくくりを置かない）を**ここで解除する。** 19 回時点では理論寄りの回が控えていて
   最終回とは限らなかったが、20 回で完結させることが決まった。
   末尾に `# 補遺` と `# 総括` を置く（構成案 8・9）。
7. **Hask 圏の bottom 問題には必ず触れる**（README.md「未定のアイデア」の圏論の項）。
   すべての型が `undefined`・非停止を要素に持つため、Hask を厳密に圏として扱うと壊れる。
   `seq` があると自然性やファンクター則が破れる。避けずに扱う。
8. **数学的な厳密さより対応関係を優先する。** 圏論の一般論（極限・随伴の一般定義など）に
   立ち入らず、**Haskell 側に対応物があるものだけを扱う。**
9. **次回予告は書かない**（16 回決定事項 12）。完結の回なので次はないが、
   「続編を書くかもしれない」といった含みも持たせない。
   **`# 補遺` は読者が自分で先へ進むための地図として書き、著者の予告としては書かない。**

## 構成案

|節|内容|回収する過去回|
|---|---|---|
|1. `# 圏`|圏の公理 ＝ `Category` 型クラス。Hask 圏と bottom|19|
|2. `# 関手`|対象の対応と射の対応。自己関手|15|
|3. `# 自然変換`|`f ~> g`。自然性条件|16・18|
|4. `# Kleisli 圏`|`instance Category (Kleisli m)` ＝ Kleisli 圏|19・15|
|5. `# モナドはモノイド対象`|★見せ場。μ と η、モノイド則|15・14|
|6. `# 随伴と自由生成`|Free の「自由」の決着|16・14|
|7. `# 米田の補題`|Coyoneda。17 回で `Functor` が不要だった理由|17|
|8. `# 補遺`|扱えなかった話題の地図||
|9. `# 総括`|全 20 回の総括（完結）||
|10. `# 参考`|書誌||

**節順の根拠**: 1〜4 は圏の言葉を揃える段。5 が見せ場。6・7 は 16・17 回の宿題を
片付ける段で、どちらも「既に書いたコードに名前が付く」型。7 を最後に置くのは、
`Coyoneda` の自作が新規コードとして最も重いため。

**`# まとめ` は置かない。`# 総括` がその役割を兼ねる。** 20 回の内容のまとめ
（「一行の主張」の対応表）を総括の冒頭に置き、そこからシリーズ全体へ広げる。
まとめと総括を並べると同じことを 2 度書くことになるため。

### 1. `# 圏`

- **入口は 19 回の `Category`。** `class Category cat where id :: cat a a;
  (.) :: cat b c -> cat a b -> cat a c` を再掲し、これが圏の定義そのものだと明かす。
  - 型 `a`・`b`・`c` が**対象**、`cat a b` が**射**、`.` が**合成**、`id` が**単位元**（恒等射）。
  - 19 回の `### 単位元` で「`id` に求められているのは恒等関数であることではなく
    単位元であること」と説明済み。**ここが最大の足場。**
- **公理は 2 つだけ**（結合律・単位律）。19 回で `id >>> f = f`・`f >>> id = f` を書いた。
  結合律 `(f >>> g) >>> h = f >>> (g >>> h)` を足す。
- 新規コードとして `instance Category` を 1 つ自作する。**19 回とは別題材にする**
  （19 回は静的パーサー `P`）。→ 題材は未決事項。
- **`## Hask 圏`**: 型を対象、関数を射とみなす圏。`instance Category (->)` がそれ。
- **`## bottom`**（決定事項 7）: すべての型が `undefined`・非停止を要素に持つので、
  Hask を厳密に圏として扱うと壊れる。`seq` があるとファンクター則・自然性が破れる。
  - 具体例をコードで示す（検証項目）。避けて通らない。
  - ⚠ **ここで読者を突き放さない。** 「だから圏論は使えない」ではなく、
    「bottom を無視すれば対応が取れる」という実務的な着地にする。

### 2. `# 関手`

- **入口は 15 回の `Functor`。** 関手は 2 つの対応の組であることを明かす。
  - **対象の対応**: 型 `a` を型 `f a` に移す（`f :: * -> *`。種は 14・16 回で既習）。
  - **射の対応**: 関数 `a -> b` を `f a -> f b` に移す（`fmap`）。
  - **`Functor` インスタンスは、この 2 つを 1 つの型構築子で兼ねている。**
    型構築子の適用が対象の対応、`fmap` が射の対応。
- **ファンクター則が「構造を保つ」の中身。** 15 回 `## ファンクター則` は
  `fmap id == id`・`fmap (f . g) == fmap f . fmap g` を「単位元」「準同型」と
  呼んでおり、`:::message` で「`fmap` は関数合成という構造を保つ準同型」とまで書いてある。
  **圏論の言葉では、合成と恒等射を保つことが関手の定義。**
- **`## 自己関手`**: Hask から Hask への関手なので**自己**（endo）関手。
  `f :: * -> *` で型が閉じているのがその現れ。**5 節の「自己関手の圏」への布石。**

### 3. `# 自然変換`

- **入口は 16 回の `foldFree`・18 回のハンドラー。** どちらも
  `forall a. f a -> g a` の形をしていた。

  ```hs
  type f ~> g = forall a. f a -> g a
  ```

- 実例を base から拾う: `listToMaybe :: [] ~> Maybe`・`maybeToList :: Maybe ~> []`・
  `Just :: Identity ~> Maybe`（または `reverse` が `[] ~> []` になること）。
- **`## 自然性`**: 自然性条件を等式で書き、実際に両辺を評価して一致を見せる。

  ```hs
  fmap h . alpha == alpha . fmap h
  ```

  「先に中身を書き換えてから容れ物を移しても、容れ物を移してから中身を書き換えても同じ」。
- **`:::message` でパラメトリシティ。** Haskell では `forall a.` の形をした関数は
  自然性条件を自動的に満たす（bottom を除く）。型だけから等式が出るという話で、
  README.md「未定のアイデア」のパラメトリシティの項目にあたる。**深入りしない。**
- ⚠ **`~>` は自作する**（決定事項 3）。`TypeOperators` の要否は検証項目。

### 4. `# Kleisli 圏`

- **19 回で書いた `instance Monad m => Category (Kleisli m)` が Kleisli 圏そのもの。**
  対象は Hask と同じ型、射は `a -> m b`、合成は `>=>`、恒等射は `return`。
- **15 回 `### >=> で書き直す` が圏の公理そのものだったことを明かす。**
  あの節はモナド則を次の 3 本に書き直して「`>=>` を掛け算だと思えば `return` は 1」
  と締めている。

  ```hs
  return >=> f    == f                -- 左単位元
  f >=> return    == f                -- 右単位元
  (f >=> g) >=> h == f >=> (g >=> h)  -- 結合法則
  ```

  **これは 1 節で書いた圏の公理（結合律・単位律）と同じ形。**
  つまり**モナド則とは「Kleisli 圏が圏であること」**の要求だった。
- 動作確認は 19 回と別題材にする（19 回は `parse`／`half` の `Maybe`）。→ 未決事項。
- ⚠ この節は**分量を抑える。** 19 回と 15 回の再確認が主で、新出は「Kleisli 圏」という
  名前だけ。5 節への助走として短く通す。

### 5. `# モナドはモノイド対象`（★見せ場）

**「モナドは自己関手の圏におけるモノイド対象である」という有名な一文を、
語ごとに分解して回収する。**

- **`## join`**: `>>=` の代わりに `join :: m (m a) -> m a` を主役に据える。
  `>>=` と `join` が相互に定義できることをコードで示す（検証項目）。

  ```hs
  m >>= k  = join (fmap k m)
  join mm  = mm >>= id
  ```

- **`## μ と η`**: `join` と `return` を**自然変換として**読み直す。
  - `join :: m . m ~> m`（μ）
  - `return :: Identity ~> m`（η）
  - 関手を掛け算のように並べた `m . m` が「2 つ重ねた `m`」にあたること、
    `Identity` が単位元の位置にあることを説明する。
- **`## モノイド則`**: 14 回の `Monoid`（`<>` と `mempty`、結合律と単位律）と並べる。

  |`Monoid`|モナド|
  |---|---|
  |`<>  :: a -> a -> a`|`join   :: m (m a) -> m a`|
  |`mempty :: a`|`return :: a -> m a`|
  |`(x <> y) <> z == x <> (y <> z)`|`join . join == join . fmap join`|
  |`mempty <> x == x`・`x <> mempty == x`|`join . return == id`・`join . fmap return == id`|

  等式を実際に評価して一致を見せる（検証項目）。
- **`## 一文を読み解く`**: 「自己関手の圏」＝ 対象が自己関手、射が自然変換の圏。
  「モノイド対象」＝ その圏で `<>` と `mempty` にあたる射（μ と η）を持ち、
  モノイド則を満たすもの。**2 節の自己関手・3 節の自然変換・14 回のモノイドが
  ここで全部使われる。**
- ⚠ **ジョークとして消費しない。** この一文は「何を言っているか分からない例」として
  引用されがちだが、20 回は**分解して意味を通す**のが目的。茶化さない。
- ⚠ **出典を裏取りする**（下記「参考文献」）。記憶で経緯を書かない。

### 6. `# 随伴と自由生成`

**16 回 `# 「自由」とは何か` 末尾の `:::message` の回収。**
あそこには既に「忘却関手の左随伴」と書いてあり、**20 回で説明すると予告した形になっている。**

- **`## 忘却関手`**: モノイドから中身の集合だけを取り出す対応。構造を忘れるので「忘却」。
- **`## 自由関手`**: その逆向きに、集合からリスト（自由モノイド）を作る対応。
  **この 2 つが随伴の関係にある**、というのが「自由」の正確な意味。
- **`## 普遍性`**: 随伴が持つ性質を、Haskell の関数の形で見せる。
  - 集合 `a` からモノイド `m` への関数 `a -> m` を 1 つ与えると、
    `[a] -> m` が**一意に決まる**。それが `foldMap`。
  - 命令の型 `f` からモナド `m` への自然変換 `f ~> m` を 1 つ与えると、
    `Free f a -> m a` が**一意に決まる**。それが 16 回の `foldFree`。
  - **この 2 つを並べて見せるのがこの節の核心。**

  ```hs
  foldMap  :: Monoid m => (a -> m)  -> [a]      -> m
  foldFree :: Monad  m => (f ~> m)  -> Free f a -> m a
  ```

- 16 回の `foldFree` を再実装して実際に動かす（検証項目）。
- ⚠ **随伴の一般定義（`Hom(F a, b) ≅ Hom(a, G b)`）には踏み込むかどうか未決。**
  上記の「1 つ与えると一意に決まる」がその内容そのものなので、
  形だけ示して名前を添える程度に留める案が有力。→ 未決事項。

### 7. `# 米田の補題`

**17 回 `## 継続を命令の型から外す` の回収。**
あの節で `Program` に `Functor instr =>` 制約が要らなかった理由が、ここで判明する。

- **`## Coyoneda`**: 17 回の `:>>=` と同じ形の型を自作する。

  ```hs
  data Coyoneda f a = forall b. Coyoneda (f b) (b -> a)
  ```

  ```hs
  (:>>=) :: instr b -> (b -> Program instr a) -> Program instr a  -- 17 回
  ```

  **「容れ物と、中身を変換する関数」の組**という形が共通している。
- **`instance Functor (Coyoneda f)` は `f` に何も要求しない。** `fmap` は
  後ろの関数を合成するだけで済む。**これが 17 回で `Functor` インスタンスが
  不要だった理由。** 任意の型構築子を `Functor` にしてしまう構成になっている。

  ```hs
  instance Functor (Coyoneda f) where
      fmap h (Coyoneda fb g) = Coyoneda fb (h . g)
  ```

- **`lowerCoyoneda` にだけ `Functor f` が要る。** 包むのは無条件、
  取り出すときに初めて必要になる、という非対称を見せる。
- **`## Yoneda`**: 向きを逆にした型。

  ```hs
  newtype Yoneda f a = Yoneda (forall b. (a -> b) -> f b)
  ```

  `liftYoneda`／`lowerYoneda` が相互に逆であること（往復すると元に戻ること）を
  コードで確かめる（検証項目）。**`forall b. (a -> b) -> f b` と `f a` が
  同じ情報を持つ**というのが米田の補題の Haskell 版。
- ⚠ **補題の一般形（`Nat(Hom(a, -), F) ≅ F a`）には立ち入らない**（決定事項 8）。
  上の往復がその特殊形だと述べるに留める。
- `Data.Functor.Coyoneda`・`Data.Functor.Yoneda` が kan-extensions パッケージにあることを
  紹介する（コードは載せない。決定事項 3）。

### 8. `# 補遺`

**シリーズで扱わなかった話題を、読者が自分で先へ進むための地図として整理する。**
著者の予告としては書かない（決定事項 9）。
README.md の「網羅性の穴」「未定のアイデア」がそのまま材料になる。

|話題|扱わなかった理由|どこから始めるか|
|---|---|---|
|`Foldable`・`Traversable`|15 回が `Functor`・`Applicative`・`Monad` の 3 段で止まったため。`traverse` は `Applicative` の最大の実用例|15 回の続きとして `foldMap`・`traverse`|
|`Alternative`・`MonadFail`|08・09・12 回で先送りされたまま|`Maybe` の選択・パーサーの選択|
|遅延評価の理論（サンク・WHNF・正格性・space leak）|専用回を設けなかった。**知らないとコードが壊れる唯一の理論項目**|`foldl` と `foldl'`・`seq`・`BangPatterns`|
|パラメトリシティと自由定理|型が仕様になるという主張の裏付け。**3 節の自然変換と地続き**|`id :: a -> a` は恒等関数しかありえない|
|カリー＝ハワード同型対応|型と命題、プログラムと証明の対応|02 回の代数的データ型（直積・直和・関数型）|
|ヒンドリー・ミルナー型推論|型推論の結果は使い続けたが裏側に触れなかった|単一化と let 多相|
|F 代数と再帰スキーム（`Fix`・catamorphism）|16 回の Free の親戚だが分量の都合で見送った|`Free f` から「変数」を取ると `Fix f`|
|`ArrowLoop`／`rec` と FRP|19 回決定事項 2・4 で除外。遅延評価の理解が前提になる|Yampa|
|Codensity／Church エンコードによる Free の高速化|13・16・18 回で先送り。性能の話は別筋|左結合 `>>=` の二乗問題|
|`Comonad`|実用場面が限られる|`extract`・`duplicate`|
|STM など並行処理|README.md「構想」で範囲外と定めた||

⚠ **表だけにせず、なぜ扱わなかったのかを地の文で一段落。**
「入門として準備が重すぎるものを外してきた」という一貫した基準があったことを示す。

### 9. `# 総括`

- **20 回の内容のまとめから入る**（「一行の主張」の対応表）。
- **シリーズが辿った道のり。**

  |段階|回|内容|
  |---|---|---|
  |言語の基礎|01〜05|型・代数的データ型・アクション・ラムダ|
  |モナドを使う|06〜12|IO・リスト・Maybe・状態系・変換子・例外・構文解析|
  |モナドの仕組み|13〜15|継続・型クラス・自作|
  |組み立てと解釈の分離|16〜18|Free・Operational・Eff|
  |モナドを離れる|19|アロー|
  |後から名前を与える|20|圏論|

- **一貫した方針を振り返る。** README.md「動機」の「あまり数学に寄せず、
  他のプログラミング言語と同じような接し方をする」を明示し、
  **だからこそ圏論を最後に置いた**という構成の理由を述べる。
  19 回までの「圏論には言及しません」は**放棄ではなく順序の問題だった**、という締め方。
- ⚠ **感傷的にしない。** 謝辞や自分語りに寄らず、読者が何を持ち帰ったかを書く。
- ⚠ **20 回だけを読んだ読者にも意味が通るようにする**（過去記事を読んでいなくても
  分かるように書く、というシリーズの方針は最後まで維持する）。

### 10. `# 参考`

論文・書籍。書誌は下記「参考文献」。19 回に倣って番号付きリストにする。

## 分量リスク

7 節に補遺と総括が加わるので、**15 回（1252 行）・18 回（1271 行）を超える恐れがある。**

- ⚠ **完結の回なので次に回す先がない**（19 回までのように「20 回以降へ引き継ぐ」ができない）。
  落とすなら `# 補遺` に項目として立てて済ませる。
- 落とす候補は **6 節（随伴）または 7 節（米田）**。どちらも節の独立性が高い。
- ⚠ **総括と補遺は削らない。** 完結の回として最も落とせない部分なので、
  削る対象は本編側にする。
- 執筆時に初稿の行数を見て判断する。

## 練習を出さないことの補償

**練習問題は置かない**（決定事項 5）。ただし「読み物」が「眺めるだけ」に
ならないよう、次の形で手を動かす余地を残す。

- **本文のコードはすべて `runghc` でそのまま動く完結したファイルにする。**
  断片を並べず、実行結果まで載せる（13〜19 回と同じ）。読者が写して動かせば確認になる。
- **「実際に確かめる」形の小節を本文に織り込む。** 練習として問うのではなく、
  著者が確かめてみせる。たとえば自然性条件が成り立つことを両辺を評価して見せる、
  `Yoneda` の往復が元に戻ることを見せる、`seq` でファンクター則が破れることを見せる。
- 検証コードは従来どおり `check/20-*/` に置く。
  **練習がない分、本文掲載コードと検証コードは 1 対 1 に近くなる。**

## 前提知識

### 既習の材料

**執筆時は必ず各箇所を開いて、当時の言い回しに繋がる形で書くこと。**

|材料|出典|20 回での使いどころ|
|---|---|---|
|`class` / `instance` / スーパークラス / 種|`14-type-classes.md`|全編の土台|
|`Semigroup`・`Monoid`・単位元|〃 `## Semigroup と Monoid`|**モノイド対象**|
|`Functor` とファンクター則（「準同型」の語も既出）|`15-monads-and-friends.md` `## ファンクター則`|**関手**|
|モナド則と `>=>` による書き直し|〃 `### >=> で書き直す`|**Kleisli 圏・モノイド対象**|
|`Free`・`foldFree`・「自由」の説明|`16-free-monad.md` `# 「自由」とは何か`|**随伴と自由生成**|
|`instr :>>= k`・存在型・GADT|`17-operational-monad.md` `## 継続を命令の型から外す`|**Coyoneda**|
|ハンドラー（`f ~> g` の形）|`18-eff-monad.md`|**自然変換**|
|`Category`・`id` は単位元・`Kleisli`|`19-arrow.md` `## Category`・`## Kleisli`|**圏・Kleisli 圏**|
|`Maybe`・リスト|`07`・`08` 回|自然変換の実例|
|`Identity`|`15-monads-and-friends.md` `## Identity`|η の型|

⚠ **19 回に圏論回への前振りが既に入っている**（19-PLAN.md「20 回以降への引き継ぎ」）。
`## Category` と `## Kleisli` にそれぞれ置かれた `### 単位元` で、
**「`id` は恒等関数ではなく単位元である」**という見方を作ってある。
19 回の側は圏論の語を一切出していないので、**前振りだと明かすのは 20 回の側。**

### 未出（20 回で初出になるもの）

|初出|重さ|扱い|
|---|---|---|
|対象・射・圏|軽|`Category` の型に名前を割り当てるだけ|
|Hask 圏と bottom|中|**避けずに扱う**（決定事項 7）。実務的な着地にする|
|自己関手|軽|`f :: * -> *` で閉じていることの言い換え|
|自然変換・自然性|中|`~>` を自作して実例で確かめる|
|モノイド対象・μ・η|中|**見せ場。** 14 回の `Monoid` と並べる|
|忘却関手・自由関手・随伴・普遍性|重|**踏み込む深さは未決。** `foldMap`／`foldFree` の並置に留める案|
|Coyoneda・Yoneda・米田の補題|重|**コードで示し、補題の一般形には立ち入らない**|

⚠ **6・7 節が重い。** 1〜5 節は既習コードへの名前づけで済むが、この 2 つは
新規コードと新概念が同時に来る。**分量リスクの調整弁もここ**（上記）。

## 検証項目

**執筆前に実際に動かして確定させる**（19 回と同じ手順）。環境は `runghc` のみ、
外部パッケージなし（決定事項 3）。結果は各 `check/20-*/README.md` に表で残す。

### 言語拡張

- [ ] `type f ~> g = forall a. f a -> g a` に必要な拡張
      （`RankNTypes`・`TypeOperators` の要否。GHC2021 に含まれるか）
- [ ] `data Coyoneda f a = forall b. Coyoneda (f b) (b -> a)` に必要な拡張
      （`ExistentialQuantification` か `GADTs` か。GHC2021 に含まれるか）
- [ ] `newtype Yoneda f a = Yoneda (forall b. (a -> b) -> f b)` に必要な拡張
- [ ] **各ファイルを `runghc -XHaskell2010` に掛け、拡張を 1 つずつ外して必要性を洗い出す**

### コード

- [ ] 1 節の自作 `instance Category`（19 回の静的パーサーとは別題材）
- [ ] `seq` でファンクター則を破る具体例（bottom の節の裏付け）
- [ ] 自然性条件 `fmap h . alpha == alpha . fmap h` を `listToMaybe` などで両辺評価
- [ ] `join` ↔ `>>=` の相互定義が一致すること
- [ ] μ・η のモノイド則（`join . join == join . fmap join` ほか）を実際に評価
- [ ] 16 回の `foldFree` を再実装し、`foldMap` と並べた形が成立すること
- [ ] `Coyoneda` の `Functor` インスタンスが `f` に制約を要求しないこと、
      `lowerCoyoneda` にだけ `Functor f` が必要になること
- [ ] `Yoneda` の往復（`liftYoneda`／`lowerYoneda`）が元に戻ること

### 置き場所

`check/20-*/` に分けて置き、各ディレクトリに `README.md`（CLAUDE.md「検証コード」）。
案: `20-category`・`20-functor`・`20-natural`・`20-kleisli`・`20-monoid`・
`20-free`・`20-yoneda`。**節の構成が固まってから確定する。**

## 参考文献

**書誌の確認状況を明記する**（17〜19 回の方針）。
⚠ **記憶で書かない。確認できたものだけ載せ、確認できなければ記述ごと落とす。**

|文献|状態|
|---|---|
|Mac Lane, *Categories for the Working Mathematician*, Springer|⚠ **未確認**（版・年・該当章）|
|Awodey, *Category Theory*, Oxford University Press|⚠ **未確認**（版・年）|
|Bartosz Milewski, *Category Theory for Programmers*|⚠ **未確認**（オンライン版の URL・書籍版の書誌）|
|Moggi, "Notions of Computation and Monads" (1991)|⚠ **未確認**（掲載誌・巻号・ページ）|
|「モナドは自己関手の圏におけるモノイド対象」の出典と流布の経緯|⚠ **未確認。** James Iry のブログ記事が広めたとされるが**記憶ベース。** 元の一文が Mac Lane のどこにあたるかも含めて要裏取り|
|kan-extensions パッケージ（`Data.Functor.Coyoneda`・`Data.Functor.Yoneda`）|⚠ **未確認**（Hackage の現況・lts-24 系での収録）|

- 16 回・17 回・19 回の `# 参考` に既に載せた文献（Swierstra "Data types à la carte"、
  Kiselyov ほか、Hughes、Paterson）と重複しないよう確認する。

## 未決事項・TODO

- [ ] **1 節で自作する `Category` インスタンスの題材**（19 回の静的パーサーとは別のもの）
- [ ] **4 節の動作確認の題材**（19 回の `parse`／`half` とは別のもの）
- [ ] mermaid で可換図式を描くか（決定事項 2）。描くなら何を描くか
- [ ] 6 節（随伴）で随伴の一般形（`Hom(F a, b) ≅ Hom(a, G b)`）まで示すか、
      `foldMap`／`foldFree` の並置に留めるか
- [ ] 7 節で `Yoneda` の往復まで踏み込むか、`Coyoneda` だけで止めるか
- [ ] 6 節・7 節のどちらかを落とす必要が出た場合の判断基準（上記「分量リスク」）
- [ ] 「圏論には言及しない」方針の解除をどう書くか（冒頭の言い回し）。
      **総括で回収する伏線になるので、冒頭と総括を対にして書く**
- [ ] 補遺の項目をどこまで挙げるか（README.md の「網羅性の穴」「未定のアイデア」を
      全部載せるか、絞るか）
- [ ] 総括で 01〜20 回を個別に列挙するか、段階（6 つの区分）にまとめるか
- [ ] 番外編 `monad-laws.md`・`monad-laws-2.md` を読み、重複と接続を確認する。
      総括や補遺から番外編・応用編へ言及するかも決める
- [ ] 検証コードを書いて `check/20-*/` に整理する（上記「検証項目」）
- [ ] 本文の初稿を書く
- [ ] `README.md` の目次・`PREFACES.md`・`ARTICLES.tsv` を更新する
- [ ] **完結に伴う `README.md` の書き直し**（下記「完結に伴う作業」）
- [ ] 推敲・校正
- [ ] フロントマターの `url` に Zenn の記事 URL を入れる
- [ ] Zenn へ複製し `ZENN.tsv` に追記する
- [ ] 各記事の目次にある 20 回の行をリンクに差し替える（01〜19 回すべて）

## 完結に伴う作業

**20 回はシリーズの完結なので、[README.md](README.md) の更新範囲が普段より広い。**

- 目次に 20 回を追加する（13 回以降と同じく「（Zenn で公開）」）。
- **「構想」「未定のアイデア」「網羅性の穴」を完結後の記述へ書き直す。**
  20 回の `# 補遺` と内容が重複するので、**README 側は補遺を指すだけにする案**が有力。
  - 「構想」の「ゴールはアロー」「20 回として検討中」は達成済みなので過去形に直す。
  - 「未定のアイデア」は補遺へ移った形になる。README には残すか、
    「補遺に整理した」と書いて畳むか要判断。
- 19-PLAN.md「20 回以降への引き継ぎ」は **20 回の `# 補遺` が引き取る。**
  20-PLAN.md には引き継ぎ節を設けない（次がないため）。

## 公開方針

[README.md](README.md) の「公開方針（13回以降）」に従う。13〜19 回と同じ扱い。

- 執筆はこの Qiita 側リポジトリ、公開は Zenn。`id` は空のまま、`url` は Zenn の
  スラッグが決まった時点で埋める。
- 補足は Zenn 記法の `:::message`。**`:::details` は使わない**（解答例専用の記法で、
  20 回に練習がないため。決定事項 5）。
- 検証コードは `check/20-*/` に置き、各ディレクトリに `README.md`。
  **base だけで完結する**（決定事項 3）ので、すべて `runghc` で動く。
  stack の `:::message`・resolver の断りは不要になる見込み。
- 言語拡張の基準は GHC2021。載るプラグマは検証で確定させる（検証項目）。
- **言語拡張の書き方への 👉リンクは張らない**（17-PLAN.md 推敲時の変更点 2）。

## 既存記事との関係

- **番外編 [モナド則の絵を描いてみた](monad-laws.md)・
  [モナド則がちょっと分かった？](monad-laws-2.md)** が近い題材。
  **執筆前に読んで重複と接続を確認する**（TODO）。
  総括や補遺から言及するかも合わせて判断する。
- 16 回 `# 「自由」とは何か` の末尾 `:::message` は**既に随伴に踏み込んでいる。**
  6 節はそこを本編に引き上げる形になるので、**16 回の記述と矛盾しないか確認する。**
- 19 回 `## Category`・`## Kleisli` の `### 単位元` は
  **意図的に置かれた前振り**（19-PLAN.md 執筆時の変更点 8）。1 節・4 節で回収する。
- 17 回 `## 継続を命令の型から外す` の `:::message`（Freer の命名）とは別の話。
  7 節は `Functor` インスタンスが不要だった**理由**の側を扱う。

## 推敲時の観点

16〜19 回から引き継ぐ。

- 半角スペースのルール・ダッシュ禁止・「要ります」禁止（[CLAUDE.md](../../CLAUDE.md)）。
- ボールドは新出用語の定義箇所にのみ使う（CLAUDE.md）。
  **20 回は新出用語が多いので特に注意。** 主張の強調に使わない。
- 掲載コードと `check/20-*/` のファイルが一致しているか。実行結果も実際の出力と一致するか。
- 過去記事を「第N回」のような回数で参照していないか。
- 他記事へのリンクが行末の `👉[省略タイトル](URL#アンカー)` 形式に揃っているか。
  省略タイトルは 14 回が `型クラス`、15 回が `モナドとゆかいな仲間たち`、
  16 回が `Freeモナド`、17 回が `Operationalモナド`、19 回が `アロー`。
- **過去記事を読んでいない読者が置いていかれていないか。** 20 回は回収の回なので
  過去記事への依存が最も高い。**各節の冒頭で該当箇所を独立して説明し直してからリンクする**
  （README.md「スタイル」）。
- **理論から入る書き方になっていないか**（決定事項 1 の逆向き構成）。
  抽象的な定義で節が始まっていないか。
- **厳密さを装って嘘を書いていないか**（Hask 圏と bottom。決定事項 7・8）。
- **「モナドは自己関手の圏におけるモノイド対象」をジョークとして消費していないか。**
- **総括が感傷的・自分語りになっていないか**（決定事項 6）。
- **補遺が予告になっていないか**（決定事項 9）。
- 練習が紛れ込んでいないか、`:::details` を使っていないか（決定事項 5）。

## 補足

- 検証コードは `series/haskell-intro/check/20-*/` に置く（CLAUDE.md の「検証コード」の方式）。
- このファイルは拡張子を除く部分が大文字・数字・ハイフンのみなので
  `ARTICLES.tsv` の収集対象から除外される。
