# haskell-intro シリーズ リンク作業メモ

`series/haskell-intro/` の 01〜15回について、記事間の話題参照リンク(前方参照・後方参照)を整備する作業の途中経過。作業を再開する際はこのファイルを起点にする。

## 現状(未コミット)

01〜15回の本文に、記事間の話題参照リンクを仮リンク記法で追加・統一済み(未コミット、`git status` で確認可能)。

```
👉[]({2桁}#見出しそのまま)
```

- リンクテキストは空。`{2桁}` はリンク先の回数(例: `03`, `15`)。
- `#` 以降は `anchor.py` によるパーセントエンコードをしていない、見出しの生文字列。
- 実 URL(Qiita/Zenn の別、id、相対パス)への変換は**まだ行っていない**。
- 全 15 ファイルで合計 62 件のリンクを付与・統一済み(内訳は各ファイルで `grep -c '👉\[\](' ファイル名` で確認できる)。

### やったこと

1. 01〜12回の本文に元々あった、書式がバラバラな記事間参照(`詳細は続編の[記事タイトル](URL)で説明します` のような文中リンクや、`[見出し名](URL#アンカー)` のようにアンカー付きだったものなど)を、上記の仮リンク記法に統一。
2. 13〜15回に元々あった `👉[記事タイトル](URL)` 形式(記事トップへのリンク、アンカーなし)も同じ仮リンク記法に変換。
3. まだリンクが張られていなかった「後で回収された話題」「過去に説明済みの話題への言及」にも、同じ書式で新規にリンクを追加。
4. 01〜12回は本文を書き換えたため `updated_at` を `''` にリセット済み([CLAUDE.md](CLAUDE.md) の同期ルールに従う)。13〜15回は元々 `id`・`url`・`updated_at` が空のため変更なし。

### 対象外にしたもの(触っていない)

- シリーズ外部の記事(8086逆アセンブラ、モナド則の絵、C++11/Javaパーサコンビネータ超入門、Twitter (`@7shi`) 参照、JavaScript/Scheme/mathlog の参考リンクなど)。
- 各記事冒頭の「シリーズの記事です」番号付き目次リスト、末尾の「参考」「関連記事」節。
- 解答例ファイル(`01a-intro.md` など 0Na 系)本体、および `⇒ [解答例](url#アンカー)` 形式の自記事内リンク。
- 15回の「まとめ」「参考」節にある番外編記事へのリンク(`monad-laws-2.md` など。haskell-intro シリーズ外)。

### 検証済みの内容

- 全 63 件のリンクについて、`{2桁}#見出し` の見出し部分が対象ファイルの実際の Markdown 見出しと一致することを確認済み(スペースを含む見出し `class と instance`・`Semigroup と Monoid` の表記ゆれも修正済み)。
- 01〜12回の `updated_at` が `''` になっていることを確認済み。

## 今後の作業予定

### 1. 仮リンクを実 URL に変換する(未着手)

`{2桁}#見出し` を実際のリンクに変換する。変換規則:

| 回 | 変換先 |
|---|---|
| 01〜12 | `https://qiita.com/7shi/items/{id}` (`id` は各記事の front matter、または [README.md](series/haskell-intro/README.md) のシリーズ目次に記載) |
| 13 | `https://zenn.dev/7shi/articles/20260803-haskell-continuation-monad` |
| 14 | `https://zenn.dev/7shi/articles/20260805-haskell-type-classes` |
| 15 | 未公開(`id`・`url` 空)。相対パス `15-monads-and-friends.md#アンカー` にする([CLAUDE.md](CLAUDE.md) の「記事間のリンク」節に従う。投稿後に URL へ差し替え) |

見出し部分は `uv run scripts/anchor.py "見出しテキスト"` でパーセントエンコードされたアンカーに変換する(同一記事内に同名見出しが複数ある場合は `-1`・`-2` の連番に注意)。リンクテキスト(空にしてある `[]` の中身)も、README.md のシリーズ目次に合わせた記事タイトル(例: `Haskell アクション 超入門`)を補う。

機械的な変換のため、スクリプト化してから流し込むのが安全(手作業だと id やアンカーの転記ミスが起きやすい)。

### 2. 13〜15回を Zenn 側へ反映する(1. の後)

仮リンクのままだと Zenn 側に無効なリンクが伝播するため、実 URL への変換が終わってから着手する。

1. `ZENN.tsv` に 13・14・15 が登録されているか確認する。
2. 登録されていれば、Qiita 側ファイルを `touch` して mtime を更新し、`make sync -n` で差分確認後 `make sync` を実行する。

### 3. 解答例ファイル(0Na)への展開は未定

今回のセッションでは解答例ファイルはスコープ外とした。必要になれば別途判断する。

## 参考

- アンカー変換規則: [scripts/anchor.py](scripts/anchor.py) の docstring、実例は [anchor-sample.md](anchor-sample.md)
